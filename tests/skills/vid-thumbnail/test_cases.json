{
  "skill": "vid-thumbnail-v2",
  "corpus": "../../corpus/seeds.json",
  "fixtures": "../../fixtures/shared",
  "input_stage": "fixtures/after-title",
  "note": "vid-thumbnail-v2 generates exactly ten strong, meaningfully distinct thumbnail-text options against one locked title, presents all ten, and lets the creator choose exactly three tests to save. Text only, no visual design. Inputs are the shared foundation and banks plus a per-seed after-title state: piece.md with frame, core_payoff, format, goal, and the locked title; brain-dump.md supplies factual provenance and source material. The suite-local stage lives at tests/skills/vid-thumbnail/fixtures/after-title/{slug}/. The runner drives vid-thumbnail-v2 once per seed with the creator-simulator playing Sam Rivera. Per case, outputs/case_NN/piece.md must preserve the after-title piece while adding exactly three creator-selected thumbnail_text values, exactly three aligned thumbnail_shape measurement labels, and an updated last_updated value. outputs/case_NN/transcript.md must show the locked title once, then exactly ten numbered options in the form N. **\"TEXT\"**: Tests whether {brief viewer-facing hypothesis}, followed by the creator choosing exactly three option numbers. The three saved texts must be distinct, verbatim options from the shown ten; saved order follows the creator's selection. Pattern labels and research stay private. Shared title words are allowed; only an option that duplicates the full title is a deterministic Tier A package failure, while semantic repetition is judged in Tier B. Hard rules under test: exactly ten shown options, exactly three saved tests, aligned shape count, one to four words usual and five maximum, numbers trace verbatim to the material, no listed anti-pattern words, and every saved test came from the shown set. Adversarial seeds case_04 and case_05 have no numbers and no authorized bank pulls, so any digit in a shown or saved test is a fabrication failure."
}
