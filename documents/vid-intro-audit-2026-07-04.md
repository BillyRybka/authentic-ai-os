# vid-intro structure audit, 2026-07-04

First full run of the skill-auditor design (documents/skill-auditor-design.md) against vid-intro. 4 lens finders, 1 skeptic per finding, 2 sweep critics, 56 agents total. Both criticals hand-verified by the orchestrator. Findings below are deduplicated only in the summary doc; overlapping lens findings are kept here in full.

## Critical (2)
### [T2] Standalone entry runs before vid-structure, then vid-structure's skeleton write clobbers the locked intro
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 18 ('A video has a locked title and thumbnail and now needs the opening written'), line 10 (hard stop only on title/thumbnail), lines 24-29 (prerequisites list foundation, piece.md title+format, thumbnail-brief, brain-dump; no script.md skeleton or segment_purposes requirement). Versus vid-structure SKILL.md line 14 (script.md with 'An empty ## Intro' is vid-structure's OUTPUT, written 'per assets/script-skeleton-template.md' at line 61, with no preserve-existing clause) and vid-pipeline SKILL.md lines 87-88 (vid-structure routes before vid-intro).

**Outcome:** The skill explicitly invites standalone runs right after thumbnail lock, before the outline exists. Two concrete failures: (1) Phase 3 (SKILL.md line 168, 'Hook forward into the first body point') writes a transition to a first point that does not exist yet; vid-structure later orders points so the title pays off late, so the locked transition forwards to the wrong point. (2) vid-intro creates script.md containing only ## Intro; when vid-structure runs next it writes script.md per its skeleton template (empty intro stub), destroying the locked intro while piece.md still says intro_locked: true, so the pipeline routes straight past vid-intro to vid-segment and the video proceeds with no intro. The missing redirect is: no outline, hard stop, run vid-structure first.

**Skeptic:** Every cited line verified. vid-intro SKILL.md line 18 lists 'a locked title and thumbnail and now needs the opening written' as a standalone entry condition, which is exactly the pre-structure state; line 10 hard-stops only on title/thumbnail; prerequisites (lines 24-29) omit script.md/segment_purposes and load item 12 (line 60) treats script.md as optional, so the skill runs willingly with no outline. The clobber path is unguarded on both sides: vid-structure's load table (lines 29-37) never reads script.md, its already-outlined guard (line 23) keys only on segment_purposes (which vid-intro never sets), and Phase 3 (line 61) writes script.md per the skeleton template whose own frontmatter says 'exact-shape-of-script.md-on-first-write' with an empty ## Intro stub (template lines 34-35). So a standalone intro run followed by vid-structure destroys the creator-approved intro while piece.md keeps intro_locked: true, and vid-pipeline (line 88) then routes past vid-intro to vid-segment on the stale flag. Refutation attempts fail: (1) the model has no natural moment to notice the existing intro since vid-structure never loads script.md; (2) vid-pressure-test's stub check (lines 25, 63) is a backstop but fires only after all segments and the ending are written, does not restore the destroyed intro, and the failure-1 variant (transition locked to a first point that vid-structure later reorders per its 'payoff late' rule, line 55/94) can survive the top-3-per-reviewer cap entirely. Severity matches the critical definition verbatim: a contract the next skill cannot fulfill (intro_locked: true over an empty stub), silent destruction of approved work, pipeline misrouting. Fix is a hard-stop redirect in vid-intro when no outline exists, or a preserve-existing clause in vid-structure Phase 3.

### [T7] vid-intro hard-requires voice-profile.md + reference-pieces, but vid-pipeline promises voice is never a blocker and orders sub-skills to skip prerequisite re-checks
**Evidence:** vid-intro SKILL.md:26 "foundation/voice-profile.md exists (the guardrail) and foundation/reference-pieces/ has at least the youtube-script context" listed under "Hard requirements"; load 5 (line 53) says voice-profile is "Always loaded" with no absence fallback. vid-pipeline SKILL.md:46-50 "If foundation/voice-profile.md doesn't exist, warn once and continue... Voice is never a blocker" and line 106 passes "voice {present|absent}. Skip pre-flight and prerequisite re-checks".

**Outcome:** A creator without a voice profile is walked by the pipeline through intake, framing, title, thumbnail, and structure, then at step 6 vid-intro either hard-stops (breaking the orchestrator's explicit "never a blocker" promise mid-chain) or, obeying the skip-re-checks instruction, proceeds and runs Phase 4 "Pass 1 (guardrail)" against a file that does not exist, a load that silently fails and then gets claimed as a passed check ("Voice pressure test: pass", line 234). Contradictory instructions, undefined behavior either way. Load 6 has a fallback ("seed from the guardrail fingerprint"); load 5 has none.

**Skeptic:** Every cited line verified real and accurately quoted. vid-intro SKILL.md:26 hard-requires foundation/voice-profile.md and reference-pieces; line 53 (load 5) is "Always loaded" with no absence branch (load 6's fallback at line 54 reads the fingerprint FROM the same missing voice-profile, so it cannot rescue load 5). vid-pipeline SKILL.md:46-50 promises "Voice is never a blocker" and that the pipeline "will draft from your foundation fingerprint," a degraded mode implemented nowhere in vid-intro; line 106 passes "voice {present|absent}. Skip pre-flight and prerequisite re-checks," actively steering vid-intro into its undefined voice-absent branch (line 135 reinforces: sub-skills skip silently). The shared contract does not save it: knowledge/voice-pressure-test.md:25-27 says Pass 1 "always runs" against voice-profile.md, and its Failure modes (lines 80-82) cover absent reference pieces only, never an absent guardrail. Attempted refutations failed: (1) the skip instruction cannot be read as "already verified present" because the pipeline verified voice as ABSENT and still says continue; (2) the trigger is the pipeline's own designed-for case, since creator-setup never scaffolds voice-profile.md (it comes from the separate vid-voice-capture skill), so a new creator hits this on their first video after five completed stages; (3) the same landmine repeats in vid-segment SKILL.md:31/61, confirming it is a systemic contract break, not a one-off. Both runtime branches are bad: hard stop mid-chain breaks the orchestrator's explicit never-a-blocker promise (and vid-intro:31's remediation points at /foundation, which does not create voice-profile.md; vid-voice-capture does, per line 381), or the skill proceeds under skip-checks and the templated confirmation at line 234 hardcodes "Voice pressure test: pass" with a required pass1_guardrail field (lines 271-278) for a check that never ran, written to piece.md where voice-pressure-test.md:76 says it feeds future voice refreshes. One quibble: "silently fails" overstates the mechanics (a Read of a missing file errors loudly), so the most probable runtime is the hard-stop branch rather than fabricated-pass. But severity holds at critical either way per the stated definitions: this is literally "a contract the next skill cannot fulfill," and the alternative branch is a claimed check that did not happen.

## Moderate (24)
### [T2] Stale orchestration claims: 'gap-fill' stage does not exist, vid-pipeline and vid-pressure-test called '(future)' though both are shipped
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 20 ('Orchestrator (vid-pipeline) invokes during the SCRIPT phase, after structure and gap-fill'), line 386 ('vid-pressure-test (future)'), line 387 ('vid-pipeline (future) is the orchestrator that calls this skill during STRUCTURE or SCRIPT phase'). Reality: 'gap-fill' appears in no shipped skill (repo grep hits only vid-intro SKILL.md and the dev doc documents/SYSTEM-MAP.md); vid-pipeline SKILL.md line 14 chains vid-structure directly to vid-intro; both .claude/skills/vid-pipeline/SKILL.md and .claude/skills/vid-pressure-test/SKILL.md exist and are shipped; vid-pipeline has no STRUCTURE/SCRIPT phases, it routes on artifact presence (route table lines 82-92). Line 387 ('STRUCTURE or SCRIPT phase') also contradicts the skill's own lines 20 and 37 ('script phase').

**Outcome:** The 'When to run this' bullet and Related skills catalog describe a pipeline that no longer exists. An executor can wait on or announce a nonexistent 'gap-fill' step, or tell the creator the pipeline/pressure-test are not available yet when both are. This is exactly the relationship-narration section T2 flags: it has already rotted once and will rot again each release.

**Skeptic:** Every cited line verifies verbatim. vid-intro SKILL.md line 20 references a 'gap-fill' step that exists in no shipped skill (repo grep hits only vid-intro and the dev doc documents/SYSTEM-MAP.md); vid-pipeline SKILL.md line 14 chains vid-structure directly to vid-intro and its route table (lines 81-92) routes on artifact presence with no STRUCTURE/SCRIPT phase names. Lines 386-387 label vid-pressure-test and vid-pipeline '(future)' though both SKILL.md files exist and vid-pipeline actively routes to both. Line 387's 'STRUCTURE or SCRIPT phase' contradicts the skill's own line 37 and frontmatter ('script phase'). The outcome follows but is bounded: execution gates (prerequisites lines 24-31, pipeline route table) never reference gap-fill, so nothing blocks or fails at runtime; the harm is creator-facing misinformation (an executor reading lines 386-387 can tell the creator the pipeline/pressure-test are not available when the pipeline is the correct next step) plus demonstrated doc rot, since the text has already gone stale once after vid-pipeline shipped. That is exactly the moderate definition (drift risk, maintainability cost), not critical (no broken output, no fabrication, no unfulfillable contract) and not minor (three false system claims plus an internal self-contradiction with a real misinformation path). Severity moderate is correctly graded.

### [T3] Invocation-modes summary contradicts Phase 5: standalone save described as voice_pressure_test only, omitting intro_locked
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 35 ('Standalone: ... save the intro to content/pieces/{slug}/script.md under ## Intro, update piece.md voice_pressure_test:, end') versus lines 223-232 (Phase 5 'Always (both modes)': intro_locked, intro_strategy, intro_hook_type, intro_credibility_form, voice_pressure_test, last_updated). vid-pipeline SKILL.md line 88 routes on 'segment_purposes set, no intro_locked -> vid-intro' and knowledge/vault-integration.md line 420 requires each skill to write its distinguishing field in BOTH modes; vid-pipeline line 132 names this exact failure ('If a route keeps firing for the same skill, the sub-skill isn't persisting its field').

**Outcome:** An executor following the mode summary (read before the phases) in a standalone run never writes intro_locked. The next /vid-pipeline run on that piece re-fires vid-intro on an already-finished intro, looping the creator through intro work again. The contradiction exists only because the mode section restates the save contract instead of deferring to Phase 5.

**Skeptic:** All cited evidence verified. vid-intro SKILL.md line 35 enumerates the standalone save contract as script.md ## Intro plus piece.md voice_pressure_test only, ending with "end", while Phase 5 (lines 221-232, "Always (both modes)") mandates six fields including intro_locked (line 227), the exact field vid-pipeline line 88 routes on; vid-pipeline line 132 and vault-integration line 420 confirm the both-modes persistence contract and name the re-firing failure mode. Strongest refutation: standalone execution still runs the full 5-phase walkthrough, Phase 5 is explicit and later, and line 14 says "piece.md fields" plural, so the deterministic claimed outcome (standalone NEVER writes intro_locked, pipeline loops) is overstated; a careful executor follows Phase 5. But the refutation does not kill the finding: line 35 is not a vague pointer, it is a specific incomplete enumeration that names the wrong field set on the routing-critical field, the sibling sub-skill line 37 defers generically ("piece.md fields") while only the standalone line enumerates, and the two statements already disagree, meaning drift has already occurred. An executor wrapping a standalone run from the modes summary has a real chance of writing only what line 35 lists, producing the re-fire vid-pipeline itself warns about. Not critical (Phase 5 usually saves it, content output unaffected), not minor (active contradiction on the orchestrator's routing field). Moderate under the "drift risk, maintainability cost" definition is the honest grade; the fix is to make line 35 defer to Phase 5 instead of restating the contract.

### [T3] Modes are callers dressed as modes, and the sub-skill 'skip the load step's questions' shortcut can bypass both creator approval gates
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md lines 33-39: standalone and sub-skill run the identical 5 phases; the only differences are the packet and line 39 ('skip the load step's questions to creator and go straight to candidate generation'). Phase 1 ('the load step') contains two mandatory creator locks: line 78 ('Wait. Lock with creator approval before moving on' for the Top 3 viewer questions) and line 92 (confirm hook-type lane). Line 157: 'Each clause maps to one of the Top 3 viewer questions locked in Phase 1.' The caller-context example (line 39: 'format=case-study, locked-title=X, thumbnail-text=Y') does not match what vid-pipeline actually passes (vid-pipeline SKILL.md line 106: '[pipeline session: pre-flight done, foundation verified, voice {present|absent}, slug={slug}. Skip pre-flight and prerequisite re-checks...]', session flags plus slug only).

**Outcome:** Read literally, pipeline mode (the primary invocation path) jumps from load to Phase 2 candidate generation, so the Top 3 viewer questions that the Setup's three on-camera promises are built on get locked without the creator ever seeing them, and the hook lane is picked unconfirmed. The pipeline only ever answers 'which slug', so there is nothing that legitimately replaces those approvals. One job plus vid-segment's phrasing ('skip questions the caller has already answered', vid-segment SKILL.md line 48) removes the ambiguity; the mode framing is what created it.

**Skeptic:** All cited lines verified exact (vid-intro SKILL.md 39, 78, 92, 157; vid-pipeline SKILL.md 106; vid-segment SKILL.md 48). The refutation attempts fail: Phase 1's only creator-facing questions ARE the two approval gates (Top 3 viewer questions lock at line 78, hook lane confirm at line 92), so "skip the load step's questions to creator and go straight to candidate generation" has no other referent, and "go straight to candidate generation" unambiguously means jump to Phase 2. Line 39 is unconditional on any caller context, and the real pipeline packet (line 106: session flags plus slug only, scoped to "pre-flight and prerequisite re-checks") does not carry format/title/thumbnail, so nothing legitimately replaces the approvals. vid-segment lines 48-50 prove the family intended the conditional form ("skip questions the caller has already answered"); vid-intro dropped the condition. Concrete outcome follows: in the primary (pipeline) path the Top 3 questions that anchor the Setup's on-camera promises get locked unseen and the hook lane picked unconfirmed, contradicting both the pipeline's own scoping and the vault's "Creator drives" rule. Not critical: Phases 2-4 still gate the actual prose (creator picks candidates, reviews the Setup, runs read-aloud), and anti-fabrication is unaffected, so no bad output is guaranteed, only rework and creator-approval drift. Moderate is the honest grade; the auditor did not inflate.

### [T2] Phase 5 writes testimonials_used 'per vault-integration' but the authority schema does not define it and vid-segment does not write it
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 233 ('update both sides ... per knowledge/vault-integration.md: piece's stories_used:/proofs_used:/testimonials_used:'). knowledge/vault-integration.md piece.md schema lines 401-403 defines only stories_used, metaphors_used, proofs_used; line 408 says 'vid-intro adds intro_locked + the intro_* fields' (no testimonials_used); the 'update both sides' rule (lines 464-475) covers stories, metaphors, proofs only. vid-segment SKILL.md line 197 updates stories_used, proofs_used, metaphors_used, frameworks_used, never testimonials.

**Outcome:** The cited authority cannot back the write, and the graph contract forks: a testimonial woven by vid-intro gets a piece-side link under an undocumented field, while the same testimonial pulled by vid-segment into a body segment gets no piece-side link at all. 'Which pieces used this testimonial' is answerable only for intros, and any future skill or query built from vault-integration.md will not know the field exists. Either vault-integration defines testimonials_used for everyone or vid-intro stops citing it as the source.

**Skeptic:** Every cited line verified real: vid-intro SKILL.md:233 instructs writing testimonials_used "per knowledge/vault-integration.md", but vault-integration.md's piece schema (lines 401-403) defines only stories_used/metaphors_used/proofs_used, line 408 enumerates vid-intro's owned fields without testimonials_used, and the "update both sides" rule (464-475) covers stories/metaphors/proofs only. vid-segment SKILL.md:197 updates piece-side fields without any testimonials field despite pulling testimonials as a block type (line 100). Repo grep shows testimonials_used exists only in vid-intro plus a 2026-05-08 audit doc that flagged the same drift, so it is a known gap that regressed. One part of the claimed outcome is overstated: vid-segment line 201 updates every pulled bank entry's used_in (testimonials included), so "which pieces used this testimonial" IS answerable via the bank side and Obsidian backlinks for both paths; the graph does not fork as badly as claimed. What survives is real: a skill citing an authority schema for a field the authority contradicts, plus inconsistent piece-side tracking between sibling skills. No runtime breakage or fabrication risk, so not critical; this is drift risk and maintainability cost, which is the definition of moderate (low end).

### [T2] Output packet handoff has no transport: vid-pipeline never carries it to vid-ending, and the schema has already drifted (vid-ending reads credibility.text, which the packet never emits)
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md lines 240-279 (40-line intro_packet 'for assembly awareness'; credibility block at lines 264-266 emits only woven_into and form; line 267 emits proof_used, singular, versus piece.md's proofs_used). Sole consumer: vid-ending SKILL.md lines 62-67 ('Sub-skill mode only: the intro packet returned by vid-intro to the caller', including line 67 'credibility.text if present', a field the packet schema does not define). Transport: vid-pipeline SKILL.md line 106 passes sub-skills only the session flags plus slug, never a stored packet; N vid-segment invocations sit between vid-intro and vid-ending; a stop/resume (vid-pipeline line 112) re-reads piece.md only, so the packet is gone. vid-ending load 12 (line 60) already recovers the same fields by reading script.md ## Intro verbatim in both modes.

**Outcome:** The packet is a cross-skill contract the orchestrator never fulfills: on any stop/resume or long segment loop it silently does not arrive, and even when it does, vid-ending is told to read a field (credibility.text) the producer never writes. Because the real data path is script.md plus piece.md (which works), the packet is duplicate schema that must be kept in sync with piece.md and voice-pressure-test and has already drifted twice (proof_used naming, missing credibility.text). Cost is maintenance and silent mismatch, not breakage, since the file fallback covers every field except problem_result.top_3_problem_anchored.

**Skeptic:** Every cited line verified. vid-intro SKILL.md lines 240-279 define the intro_packet; credibility (264-266) emits only woven_into and form, and line 267 uses proof_used while line 233 of the same file uses proofs_used for piece.md. vid-ending SKILL.md line 67 reads credibility.text "if present", and stronger than the finding claimed, line 262 explicitly cites "Schema in vid-intro SKILL.md Output packet section" as defining credibility.text, a field that schema does not contain. That is a hard cross-reference contradiction. vid-pipeline SKILL.md contains zero mentions of the packet; line 106 passes sub-skills only the session flag line plus slug, and the line 112 stop/resume path re-reads piece.md only, so the packet is genuinely lost across resume. Grep confirms vid-ending is the sole consumer. Refutation attempted: the packet is designed as ephemeral "assembly awareness" (vid-intro line 236) with files as source of truth, the "if present" qualifier degrades gracefully, and in a same-session run the packet is visible in the transcript, so "never arrives" is slightly overstated for the happy path. But that does not erase the objective defects: the consumer names a field the producer never writes (with a dangling schema pointer), the packet's naming has drifted from piece.md vocabulary within the same file, and the only specified transport (vid-pipeline's context line) omits it. Severity holds at moderate, not critical: no breakage or fabrication follows because vid-ending's load 12 (line 60) reads script.md ## Intro verbatim in both modes, recovering the hook, setup contract, poked problem, and the woven credibility line, so the anti-re-cite rule remains executable. The cost is duplicate schema that must be kept in sync and has already drifted twice, exactly the moderate definition. The auditor's severity was honest.

### [T4] All knowledge/ loads use vault-relative paths with no ${CLAUDE_PLUGIN_ROOT} resolution note, unlike six sibling skills
**Evidence:** .claude/skills/vid-intro/SKILL.md lines 49-63 ("1. `knowledge/intro-architecture.md`... 2. `knowledge/format-planners/{format}.md`... 9. `knowledge/parable-decision-matrix.md`") load 9+ knowledge files by bare relative path. Repo CLAUDE.md folder map: "knowledge/  # Plugin reference material (loaded by skills via ${CLAUDE_PLUGIN_ROOT})". Six sibling skills carry an explicit resolution preamble (e.g. vid-voice-update/SKILL.md:12 "Resolving `knowledge/` paths... Load it from `${CLAUDE_PLUGIN_ROOT}/knowledge/X.md` when running as an installed plugin"); vid-intro has none.

**Outcome:** In an installed creator vault, `knowledge/intro-architecture.md` and `knowledge/format-planners/{format}.md` do not exist at the vault-relative path. The loads are instructed as "silent", so they fail silently and the skill proceeds treating them as loaded: the intro gets generated without the format planner that the skill's own principle 5 (line 338) calls the adaptation layer, so News/Case Study/Interview intros get the generic 6-part shape instead of their format identity, with no error surfaced.

**Skeptic:** Evidence verified: vid-intro SKILL.md lines 47-63 instruct 15 "Silent loads" using bare knowledge/ paths with zero ${CLAUDE_PLUGIN_ROOT} mentions anywhere in the skill, while sibling skills (vid-voice-update:12, vid-voice-capture:15, vid-ideas:14, vid-voice-audit:12) carry an explicit resolution preamble; the finding's "six siblings" count is inflated (4 carry the preamble, 2 more only reference the variable in a pre-flight line). The premise that knowledge/ does not exist vault-relative in an installed vault is independently confirmed by the shipped installer: plugins/authentic-ai-os/skills/creator-setup/SKILL.md line 55 ("No knowledge/ (it ships with the plugin and is referenced from there)") and line 259 ("they never get copied into the workspace"). So the finding is real and matches a deliberate house convention vid-intro omits. Severity downgrade from critical to moderate for three reasons. (1) vid-intro is not in the current shipped plugin (plugins/authentic-ai-os/skills contains only creator-setup, foundation skills, vid-research, aaios-feedback), so no installed creator vault can hit this today; in the dev repo where the skill actually runs, CWD is the repo root and every bare path resolves. The bug is latent until release. (2) The "fails silently then proceeds as loaded" outcome is overstated as certainty: a failed Read errors visibly to the model, the skill's own installed path reveals the plugin root, and the scaffolded vault CLAUDE.md references ${CLAUDE_PLUGIN_ROOT}/knowledge/ twice, giving plausible runtime recovery. Proceed-from-priors is a genuine risk (which is why the preamble convention exists) but not the guaranteed outcome the critical grade assumes. (3) All eight other production skills (vid-title, vid-thumbnail, vid-structure, vid-segment, vid-ending, vid-framing, vid-capture, vid-pressure-test) share the same gap, so this is a systemic convention-drift issue with a one-paragraph fix, which is the textbook moderate definition (drift risk), not a break in the skill as it runs today.

### [T5] The three skill-local reference files are never loaded by any phase; their runtime decision logic is dead at runtime
**Evidence:** Phase 1's silent-load list (SKILL.md lines 47-63, items 1-15) contains no references/ file, and line 43's for-your-thinking list ("intro-architecture, hook-bank, transition-bank, format planner, voice-profile, reference-pieces") omits them too. They appear only in the Reference index (lines 364-366). Each file expects to run inside the flow: hook-type-selection-flow.md:72 "The decision short-list (run silently in Phase 1)", problem-result-options.md:113 "(run silently in Phase 2)". Repo-wide grep shows no other loader.

**Outcome:** The intensity-matching rule for Poke/Tease/Combine (pain-acuteness vs result-drama), the credibility form-to-slot mapping, and the four-input hook-lane cross-reference never inform generation; the skill runs on the thinner inline shorthand (e.g. line 139 "credibility most often weaves into the Problem/Result"). The exact failure modes those files exist to prevent (heavy Poke on mild pain, wrong-stage client result in the Hook, Combine when neither half is loud) go uncaught, and ~400 lines of shipped reference content is unreachable weight.

**Skeptic:** Evidence fully verified: Phase 1 silent-load list (SKILL.md lines 47-63) and the line 43 for-your-thinking list omit all three references/ files; they appear only in the Reference index (lines 364-366); both hook-type-selection-flow.md (line 72) and problem-result-options.md (line 113) self-describe as "run silently in Phase 1/2" yet no phase instruction loads them; repo grep confirms no other loader (only audit docs and a descriptive mention in knowledge/hook-bank-template.md:154). The internal contradiction is real: the files were authored to execute inside the flow and the flow never wires them in, so consumption depends on the model spontaneously consulting an index table rather than following the explicit load list every other consumed file uses. One correction to the auditor's claimed outcome: two of the three named failure modes have loaded backstops, so "never inform generation" is overstated. intro-architecture.md Step 3 (loaded, item 1) carries the compressed intensity rules (line 118 Poke when pain acute, 125 Tease when receipt impressive, 134 Combine when both), and the wrong-stage client result is guarded by item 13's story-pulling-criteria stage-match note plus format-planners/case-study.md Story Rules (which credibility-line-weaving.md cites as its own source). What is genuinely unreachable by instruction: the credibility form-to-slot mapping (intro-architecture Step 6 lists the 5 forms but no slot logic; SKILL.md line 139 gives only the "most often Problem/Result" shorthand), the format-wins/voice-breaks-ties priority, the "mild pain + mild result, reconsider the video" rule, and all worked/near-miss calibration examples. Severity: moderate is correct, not inflated. Not critical because the skill still functions, core selection logic survives via loaded files, and there is no fabrication risk or broken downstream contract. Not minor because roughly 400 lines of shipped decision content have no reliable execution path, the skill contradicts its own reference files' declared runtime role, and the exclusive content (slot mapping, tie-breaks, calibration) does degrade output quality when skipped. Fix is cheap: add the three files to the Phase 1/2 load instructions.

### [T5] SKILL.md re-teaches intro-architecture.md content that is always loaded anyway, and the two copies have already drifted
**Evidence:** Soft friction items 7-12 (SKILL.md:306-311) near-verbatim duplicate intro-architecture.md:291-296 ("Credibility Hook on small/new channels... a single dramatic result can earn it", the >3 Setup, >5s hook, >30s intro, teaching-in-intro, off-Top-3 poke items). The 3 problem/result options + pivot phrases are stated at lines 108-110 AND again at 322-323, while the index (line 366) declares those "live in intro-architecture Step 3". Hard friction 3-4 (297-298) duplicate intro-architecture:286-287; craft note 1 (330) duplicates intro-architecture:300-302. SKILL.md adds items 13-14 and the line-300 curiosity-bait reclassification, none of which exist in intro-architecture.

**Outcome:** Two sources of truth for the same friction rules, loaded into the same context every run (intro-architecture is load 1). They already disagree at the edges; the next edit to either file widens the gap and the model gets conflicting friction tiers for the same candidate.

**Skeptic:** Every cited line checks out. SKILL.md:306-311 duplicates intro-architecture.md:291-296 item for item (six soft-friction rules, near-verbatim, "Tends to" vs "Usually"); SKILL.md:297-298 duplicates intro-architecture.md:286-287; SKILL.md:330 duplicates intro-architecture.md:300-302; the 3 problem/result options + pivot phrases appear at SKILL.md:108-110 AND 322 while SKILL.md:366 declares they "live in intro-architecture Step 3". Both files load every run (intro-architecture is unconditional load 1, SKILL.md:49), so the duplication buys no availability and puts two sources of truth in the same context. Drift is not hypothetical, it already exists: (1) SKILL.md:306 says "dramatic claim" where intro-architecture:291 says "dramatic result" (the auditor's quote actually conflated the two, which confirms the drift rather than undermining the finding); (2) a live tier conflict the auditor undersold: intro-architecture:281-282 says its whole friction list including the "hard" items is "Not auto-reject. The creator can override", and line 298 warns "Locking everything as 'REJECT' stifles creativity", while SKILL.md:291-293 makes items 3-4 auto-reject where the "creator never sees them"; (3) SKILL.md's items 13-14 and the line-300 curiosity-bait reclassification exist only in one copy. Refutation attempts fail: duplication would only be defensible if intro-architecture were conditionally loaded, and it is not; the skill's own reference index states the single-ownership rule it violates. Severity stays moderate, not critical: SKILL.md is the operative procedure and its explicit REJECT dominates in practice, so the concrete harm is context bloat, contradictory friction tiers in the same context window, and a widening gap on each edit, i.e. drift/maintainability cost, not broken output or fabrication risk.

### [T7] Prerequisites omit the structure dependency, so a standalone pre-structure run gets its locked intro clobbered by vid-structure
**Evidence:** vid-intro's hard requirements (SKILL.md:24-31) demand title, thumbnail-brief, brain dump, but never script.md or segment_purposes, and line 18 invites running as soon as "a locked title and thumbnail" exist, while line 20 admits the pipeline runs it "after structure". vid-structure Phase 3 (vid-structure SKILL.md:61) writes script.md wholesale from assets/script-skeleton-template.md, whose Intro slot is the stub "*vid-intro fills this after title and thumbnail lock.*" (template line 35), with no preserve-existing-Intro instruction. Phase 3's own transition check (SKILL.md:183) needs "the brain dump's first body point", which does not exist before structure.

**Outcome:** Creator locks an intro right after thumbnail (as line 18 permits), then outlines: vid-structure rewrites script.md and silently destroys the locked ## Intro while piece.md keeps intro_locked: true, so the pipeline routes past vid-intro to vid-segment and the stub rides until vid-pressure-test hard-rejects it; the creator redoes the intro. Pre-structure, the hook-forward transition is also generated against a first body point that hasn't been planned.

**Skeptic:** All cited evidence verified real. vid-intro SKILL.md:18 and its prerequisites (lines 24-29) permit and invite a standalone run right after title+thumbnail lock, with no script.md or segment_purposes requirement (line 60 even makes script.md optional). vid-structure SKILL.md:61 writes script.md per the skeleton template including "the intro stub" (template line 35), with zero preserve-existing-Intro instruction; its only re-run guard (line 23) keys on segment_purposes, which a pre-structure intro run never sets. I traced vid-pipeline's route table: row 87 (no segment_purposes -> vid-structure) matches before anything checks intro_locked, so structure runs and clobbers the locked intro; row 88 then sees the stale intro_locked: true and routes past vid-intro to vid-segment, leaving the stub in script.md until vid-pressure-test rejects it. The concrete bad outcome (silent destruction of locked creator work plus an inconsistent piece.md state that misroutes the pipeline) follows mechanically from the actual text, not from pattern-matching. The supporting sub-claim also holds: vid-intro:183's transition check references "the brain dump's first body point," which does not exist pre-structure (minor citation sloppiness: line 183 is in vid-intro's SKILL.md, not vid-structure's, but the quote is real). Severity moderate is correct, not inflated: the canonical pipeline order never hits this (structure always precedes intro there), the failure requires a standalone out-of-order run, and vid-pressure-test eventually catches the stub, so the cost is redone work and a confusing state rather than published bad output or fabrication. Fix is cheap: either add an outline prerequisite to vid-intro or make vid-structure preserve an existing non-stub ## Intro.

### [T7] Stale pipeline claims: a phantom "gap-fill" stage and "(future)" labels on two shipped skills
**Evidence:** SKILL.md:20 "Orchestrator (vid-pipeline) invokes during the SCRIPT phase, after structure and gap-fill": repo-wide grep shows "gap-fill" exists nowhere else; vid-pipeline's chain (vid-pipeline SKILL.md:14, route table line 88) goes vid-structure directly to vid-intro. SKILL.md:386-387 label vid-pressure-test and vid-pipeline "(future)" and describe the orchestrator calling this skill "during STRUCTURE or SCRIPT phase", phases the shipped artifact-based router does not have.

**Outcome:** A model running the skill can wait for or reference a gap-fill stage that no skill performs (vid-structure hands over a `## To build` list of unsourced blocks with nothing between it and vid-intro), and in standalone mode can tell the creator the pipeline or pressure test doesn't exist yet. Description drifted from the rewritten neighbors.

**Skeptic:** Evidence verified line-for-line. vid-intro SKILL.md:20 names a "gap-fill" stage between structure and intro; repo-wide grep shows "gap-fill" exists only there and in documents/SYSTEM-MAP.md (a design doc, not a shipped skill). vid-pipeline SKILL.md:14 and its route table (line 88: segment_purposes set, no intro_locked -> vid-intro) route vid-structure directly to vid-intro with artifact-state matching and no named phases, so both the phantom stage and the "STRUCTURE or SCRIPT phase" wording at vid-intro SKILL.md:386-387 contradict the shipped router. Both vid-pipeline and vid-pressure-test exist as shipped skills with SKILL.md files, so the "(future)" labels at lines 386-387 are factually wrong. Outcome follows partially: line 20 is in the load-bearing "When to run this" section, so a model can tell the creator a nonexistent gap-fill stage must run first; and since line 386 is vid-intro's only mention of vid-pressure-test, a standalone run can wrongly tell the creator the pressure test is not built yet. The vid-pipeline "(future)" half is weaker because lines 3, 14, 20, 35, and 37 of the same file treat the pipeline as real, so a model is unlikely to deny it exists. Severity: does not break vid-intro's output or risk fabrication, so not critical; it is real drift from rewritten neighbors with a concrete chance of wrong statements to the creator, so moderate (not minor) is the honest grade. The auditor did not inflate.

### [T8] vid-intro writes testimonials_used to piece.md but vault-integration's piece schema and update-both-sides rule define only stories/metaphors/proofs
**Evidence:** SKILL.md:233 "piece's `stories_used:`/`proofs_used:`/`testimonials_used:` AND the bank entry's `used_in:`". knowledge/vault-integration.md piece.md schema (lines 401-403) defines stories_used, metaphors_used, proofs_used only; the "update both sides" rule (lines 464-475) and the authority arbitration (lines 710-712 "Piece's stories_used / metaphors_used / proofs_used: authoritative") never mention a testimonial piece-side field, though the testimonial bank schema has used_in (line 176).

**Outcome:** The contract every skill is told to honor doesn't know the field vid-intro writes: any skill or query following vault-integration strictly treats testimonials_used as undefined, and the failure-mode arbitration (which write is primary vs reflective) has no answer for testimonials, so the bidirectional-graph promise for a woven testimonial rests on an unspecified field.

**Skeptic:** Evidence verified at real lines: vid-intro SKILL.md:233 instructs writing piece-side `testimonials_used:` "per knowledge/vault-integration.md", but vault-integration.md's piece schema (lines 401-403), the "update both sides" rule (464-475), the authority arbitration (712-714), and the graph view check (688) define only stories/metaphors/proofs; only the testimonial bank schema's `used_in` (line 175) exists. The strongest refutation, that line 408 permits skills to append their own fields, fails because that line enumerates vid-intro's additions as intro_locked + intro_* only. The drift the finding predicts is already live, not hypothetical: vid-segment pulls testimonials (SKILL.md:100) but its save phase (197) writes no piece-side testimonial field, and vid-ending (192) updates only the bank's used_in, so the same event is recorded three different ways across three skills and the piece-side view of used testimonials is incomplete for segment/ending weaves. Not critical: vid-intro's own write is complete and bidirectional, Obsidian backlinks from used_in keep the graph queryable, nothing breaks or fabricates at runtime. Not minor: the contract omission has produced actual cross-skill data model inconsistency. Moderate per "drift risk, maintainability cost" is the correct grade; the auditor's outcome statement only slightly overstates (the bank-side half of the bidirectional promise IS specified).

### [T9] Save confirmation exposes the internal test name and hardcodes its result as pass
**Evidence:** .claude/skills/vid-intro/SKILL.md line 234: 'Confirm save: "Intro locked. Saved to `script.md`. Voice pressure test: pass."' But line 211 allows saves on soft-warn ("Soft warns: surface in the meta save but allow") and the packet schema at line 273 enumerates result: pass | soft-warn | soft-reject | hard-reject.

**Outcome:** Two concrete failures. (1) Internal QA machinery ("voice pressure test") surfaces in chat on every single save, breaking the invisible-machinery rule at the most visible moment of the skill. (2) The scripted line asserts "pass" unconditionally, so a soft-warn save that follows the script reports a clean pass to the creator, misrepresenting the validation status the skill just recorded in piece.md.

**Skeptic:** Evidence verified: SKILL.md line 234 scripts a literal confirmation ("Voice pressure test: pass"), line 211 allows saves on soft-warn, line 273 (and authority knowledge/voice-pressure-test.md lines 50, 68) enumerate pass|soft-warn|soft-reject|hard-reject. Half the finding is refuted: there is no "invisible machinery" rule anywhere in the skill or plugin that forbids naming the pressure test in chat; the skill openly surfaces soft rejects (line 209) and the read-aloud test (line 213), and the confirm line is authored behavior. The sibling vid-ending (line 194) omitting the test is inconsistency, not a violation. The other half stands and carries the finding: "pass" is a hardcoded literal, not a placeholder. The author uses {placeholder} syntax elsewhere (lines 71-74, 198) and did not here, so a template-following model on the designed, reachable soft-warn save path reports "pass" in chat while recording soft-warn in piece.md, hiding the flag from the creator (soft warns land only in frontmatter). Lines 211 and 234 give directly conflicting instructions at the same moment. Not critical: no downstream skill reads the chat line, piece.md and the packet stay accurate, no contract breaks, no fabrication enters the script, and the misreported warning is allow-save by definition. A contained misreport plus internal contradiction is rigidity/drift: moderate.

### [T12] Frontmatter description is a behavior spec, not a router entry
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 3: "Pulls from creator-foundation, voice-profile, reference-pieces, the video's brain dump, the locked title, the thumbnail brief, and the matching format planner. Anti-fabrication. Every claim must trace to script or foundation."

**Outcome:** The load list and anti-fabrication rule do nothing for routing but sit in every session's skill index, and they have already diverged from the body: the description's pull list omits banks/hook-bank.md, banks/transition-bank.md, and the 6+ knowledge files Phase 1 actually loads (lines 47-63), so anyone trusting the description gets a wrong picture of the skill's inputs. Two homes for the same spec means every future edit must land in both or drift.

**Skeptic:** Attempted to break this three ways; it survived all three. (1) Evidence check: real. SKILL.md line 3 contains exactly the quoted spec content (pull list plus "Anti-fabrication. Every claim must trace to script or foundation."), and Phase 1 (lines 47-63) loads far more than the description lists. (2) "Rule pattern-matched where it does not bite" check: fails as a refutation, because the rule is not an imported external convention. It is the project's own documented standard: documents/skill-writing-lessons.md lines 50-58, lesson 4 "The description is for triggering, not for documenting behavior," which cites this exact anti-pattern ("Anti-fabrication..." smuggled into vid-intake's description) as something already identified and fixed there. vid-intro still carries the pre-lesson style, so this is a concrete compliance gap against the repo's own standard, not auditor taste. (3) "Drift is hypothetical" check: fails too, drift has already happened. The description says claims "must trace to script or foundation" while the body's actual rule (lines 43 and 65) is "brain dump or foundation." Two homes, two different trace rules, exactly the dual-maintenance failure the finding predicts. One part of the auditor's evidence is overstated: the pull list omitting hook-bank/transition-bank/knowledge files is normal summarization, not divergence, and nobody consumes the description as an input contract (vid-pipeline invokes by name). But that weakens only the supporting example, not the finding. Severity: not critical, since routing works fine (the description's back half is dense with trigger phrases) and output quality is unaffected. Moderate is the honest grade per the definitions: index bloat in every session, demonstrated drift, dual-home maintainability cost, and a documented house standard it violates.

### [T12] Body 'When to run this' duplicates the description and has drifted: it gates on a 'gap-fill' stage that no longer exists
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 20: "Orchestrator (vid-pipeline) invokes during the SCRIPT phase, after structure and gap-fill". Grep for 'gap-fill' across .claude\skills returns only this line; the just-rewritten vid-structure\SKILL.md has zero matches for 'gap', and vid-pipeline\SKILL.md line 88 routes on "segment_purposes set, no intro_locked -> vid-intro" with no gap-fill stage.

**Outcome:** The when-to-run condition names a pipeline stage nothing produces anymore. A model checking preconditions can stall or ask the creator about a nonexistent gap-fill step before writing the intro, and the section proves the cost of keeping when-to-use in two places: the description stayed current, the body copy went stale.

**Skeptic:** Evidence verified exactly: vid-intro SKILL.md line 20 says "after structure and gap-fill". Repo-wide grep confirms "gap-fill" exists in no shipped skill except this line; the only other hit is documents\SYSTEM-MAP.md lines 172/175, which describe the OLD vid-structure's "gap-fill seam" (batch-capture via vid-capture), proving gap-fill was a real stage that the just-rewritten vid-structure dropped (it now writes a passive "## To build" list, SKILL.md line 14, with zero occurrences of gap/fill/capture). vid-pipeline's routing table (lines 81-92, specifically line 88) routes segment_purposes -> vid-intro with no gap-fill stage. The claimed outcome is mildly inflated (the real gate is the Prerequisites section at lines 22-31, which never mentions gap-fill, so a hard stall is unlikely), and the "duplicates the description" half alone would be a normal pattern, but the drift is realized, not hypothetical: a model reading line 20 standalone can ask the creator about a step nothing performs, and a maintainer hunts for a nonexistent stage. Realized drift plus maintainability cost is exactly the moderate definition; it does not break output or risk fabrication, so not critical, and it names a nonexistent pipeline stage, so more than minor polish.

### [T14] 'Credibility woven, not bolted' stated 6 times
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md lines 3 ("Credibility woven"), 10 ("credibility woven into one of 1-3"), 147 ("NOT a separate self-introduction. Woven, not bolted"), 297 (Hard friction 3: "Bolted-on self-introduction... REJECT... never as a separate intro"), 334 (craft note 3: "Credibility is woven, not bolted... If the candidate intro feels like a bio, restructure"), 374 (Principles: "Credibility is woven, not bolted. Never write a separate self-introduction").

**Outcome:** Six statements, and they are not in sync on where the weave can go: line 10 says slots 1-3, line 139 says the three slots are Hook/Problem-Result/Setup, and the reference file credibility-line-weaving.md line 17 agrees with 139. The redundancy is exactly where the contradiction hides; a single statement pointing at intro-architecture Step 6 would remove both the bloat and the drift surface.

**Skeptic:** Evidence verified line by line and it holds. The six restatements exist at SKILL.md lines 3, 10, 147, 297, 334, 374. The contradiction is real: line 10 says credibility weaves "into one of 1-3", and per intro-architecture.md's numbering (1=Top 3 viewer questions, 2=Hook, 3=Problem/Result, 4=Setup, lines 28-38) that set wrongly excludes Setup and wrongly includes the viewer-questions element, while SKILL.md line 139, the output packet enum (line 265, "woven_into: hook | problem-result | setup | none"), references/credibility-line-weaving.md line 17, and intro-architecture.md line 187 all correctly say Hook/Problem-Result/Setup (steps 2-4). Two caveats that trim but do not kill the finding: (a) the "6 times" framing overstates it, since only lines 10 and 139 state the slot set and the contradiction is between exactly those two; the other four are consistent restatements of a hard-reject rule, and repeating hard rules across description/friction/craft/principles is deliberate design in this skill family, so the redundancy itself is weak evidence. (b) The auditor's proposed fix (point at intro-architecture Step 6) would inherit the same bug, because intro-architecture.md line 25 contains the identical "one of 1-3" error, contradicting its own diagram at line 38; SKILL.md line 10 copied the mistake from the authority file, and both need the slots named explicitly. Severity stays moderate, not minor: the drift is not hypothetical, it has already occurred (line 10 is out of sync today, and the authority file is internally inconsistent at line 25 vs 38/187). It is not critical because the practical output risk is near zero: the operative decision point (Phase 2 line 139), the packet schema, and intro-architecture Step 6 all agree, so no runtime model will actually exclude Setup or weave credibility into the viewer-questions beat. This lands squarely in the moderate definition: drift risk and maintainability cost, no bad output.

### [T15] Save contract restated 5 times, and the standalone-mode restatement drops the field vid-pipeline routes on
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 35: "Standalone: ... save the intro to content/pieces/{slug}/script.md under ## Intro, update piece.md voice_pressure_test:, end." versus Phase 5 lines 226-232 which require intro_locked, intro_strategy, intro_hook_type, intro_credibility_form, voice_pressure_test, last_updated. The same save-in-both-modes contract is also stated at lines 14, 37, 223, and 236. vid-pipeline\SKILL.md line 88 routes on "no intro_locked -> vid-intro".

**Outcome:** Line 35 is the mode-specific instruction a model actually follows in standalone runs, and it names only one of the six required piece.md fields. If Phase 5's 'Always (both modes)' block is not cross-checked, a standalone run leaves intro_locked unset and vid-pipeline re-invokes vid-intro on a finished intro. Five statements of one contract is how this incomplete copy survived.

**Skeptic:** All cited evidence verified real. SKILL.md line 35 says "update piece.md `voice_pressure_test:`, end" naming one of six required fields; Phase 5 lines 223-234 list the full set including `intro_locked`; the contract is restated at lines 14, 35, 37, 223, 236 (five copies); vid-pipeline SKILL.md line 88 routes on "no `intro_locked` -> vid-intro". Refutation attempt: Phase 5 is headed "Always (both modes)" and is the operative save step, so a model walking the phases will usually write all fields, which weakens the auditor's claim that line 35 is what a standalone run actually follows. But the finding holds at moderate: the drift is not hypothetical, line 35 already diverged from the other four copies (lines 14 and 37 say "piece.md fields" generically; line 35 narrowed to one named field with a terminal "end"), and the omitted field is the exact one the orchestrator routes on. If a run does follow line 35, the pipeline re-invokes vid-intro on a finished intro. This is textbook drift risk and maintainability cost (moderate), not critical (Phase 5's explicit block usually prevents the failure) and not minor (a divergent copy of a save contract that misstates the fields is an existing defect, not polish).

### [T15] Procedure scattered across four un-numbered peers of the walkthrough
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md: the pivot phrases appear at lines 108-110 (Phase 2) and again at line 322 ("Natural language patterns": 'The thing is...', 'I used to until...', 'But...'); the default verb 'show' appears at lines 159, 171, and 324; the Setup template appears at line 155 and again at line 323. "Intro craft notes" (326-340) and "Principles" (368-376) then restate the judgment rules from the phases a third and fourth time.

**Outcome:** The runnable procedure lives in the phases, but three trailing sections re-encode fragments of it, so an edit to a pivot phrase, verb rule, or setup template must land in 2-3 places inside this one file plus intro-architecture.md. The drift is already observable (line-8 summary and line-35 save contract, reported separately); the scattering is the mechanism producing it.

**Skeptic:** All cited evidence verified against real line numbers in SKILL.md: pivot phrases duplicated at 108-110 and 322, default verb 'show' at 159/171/324, Setup template at 155 and 323, craft notes (326-340) and Principles (368-376) restating phase judgment rules a third and fourth time, with canonical copies also in knowledge/intro-architecture.md (its lines 116, 132). The 'deliberate prompt reinforcement' defense fails because the file's own reference index (lines 364-366) enforces a no-restatement ownership rule for exactly these tokens, which the body then violates. The claimed drift mechanism is not hypothetical: line 8's phase summary misplaces credibility weaving (Phase 2 per lines 139-149) and omits Phase 4, and line 35 restates the Phase 5 save contract with one field instead of six. Not critical (copies currently agree, runtime output unaffected today), not minor (drift already observed twice). Moderate matches the definition exactly: drift risk and maintainability cost.

### [T2] Phase 4 visual-matching note requires a thumbnail visual style that no upstream artifact contains
**Evidence:** SKILL.md line 198: "Heads up: the thumbnail style is {cinematic / scrappy / studio / location}. The first SHOT of the video should match." But vid-thumbnail/SKILL.md line 10: "this skill produces text only. Layout, hero element, expression, color application... are OUT OF SCOPE", and the thumbnail-brief.md frontmatter (vid-thumbnail SKILL.md lines 149-162) carries only title_paired / strategies_tested / picks / creation_path, no visual style. The style slot intro-architecture.md lines 219-232 needs is populated by nothing in the shipped system.

**Outcome:** At runtime the model must fill the {cinematic / scrappy / studio / location} slot from data that exists nowhere in the vault. It either fabricates a style and asserts it to the creator as fact (in a skill whose brand is anti-fabrication) or silently drops a scripted step. Either way the visual-matching production note is unreliable every run.

**Skeptic:** Evidence verified line by line. vid-intro SKILL.md:198 scripts an assertion with a fill-in slot ({cinematic / scrappy / studio / location}) that no loaded artifact can populate. thumbnail-brief.md (frontmatter at vid-thumbnail SKILL.md:149-161 and full body template in assets/thumbnail-brief-template.md) carries no visual style; vid-thumbnail SKILL.md:10 explicitly scopes visuals out. The strongest refutation candidate, foundation/packaging-system.md, fails three ways: vid-intro never loads it (load list SKILL.md:47-63 and reference index :342-366 omit it), its "Thumbnail style" field records packaging strategy not production register (fixture at tests/fixtures/billy/foundation/packaging-system.md:21 says "Cognitive Dissonance as the lead...", mapping to none of the four categories), and the fixture defers design guardrails entirely (:113-115). Structurally, the thumbnail visual does not exist yet at vid-intro time (vid-thumbnail is text-only; the visual is designed later), so the slot is unfillable by design. At runtime the model must either fabricate a style and assert it as fact (in an anti-fabrication skill) or improvise a vaguer note, so the scripted step cannot be executed faithfully on any run. Severity is contained: the note is chat-only, never persisted, and the creator knows their own thumbnail style, so no downstream artifact or viewer-facing content is corrupted. Not critical, more than polish. Moderate as claimed; no inflation.

### [T7] Missing reference-pieces gets three contradictory behaviors inside the same file: hard stop, seed-and-note, and skip-Pass-2
**Evidence:** SKILL.md line 26 lists reference-pieces youtube-script as a "Hard requirement" and line 31 says to stop and redirect. But Phase 1 item 6 (line 54) says "If absent, seed from the guardrail fingerprint and note the gap", and Phase 4 (line 205) says "If no file for this voice_context, skip Pass 2 and note the gap". The authority contract agrees with the fallback, not the hard stop: knowledge/voice-profile-schema.md line 112: "If that file does not exist, seed from the voice_fingerprint and signature_phrases only, and note the gap".

**Outcome:** Nondeterministic behavior on a common state (fresh creator who ran /foundation but not vid-voice-capture): one run hard-stops and blocks the creator, another proceeds with the documented fallback. The prerequisite is the outlier against the skill's own Phase 1/Phase 4 text and the canonical load contract, so the strictest path (hard stop) blocks work the rest of the system explicitly supports.

**Skeptic:** All cited evidence verified at the stated lines. SKILL.md line 26 lists reference-pieces youtube-script under "Hard requirements", while line 54 (seed from guardrail fingerprint, note the gap), line 205 (skip Pass 2, note the gap), and the output packet at line 276 (pass2_grain: skipped-no-reference) all encode a proceed-with-fallback path with no exception for the default context. The authority contract (voice-profile-schema.md line 112) and the orchestrator (vid-pipeline SKILL.md lines 46-48, which warns once and continues even when voice-profile.md itself is missing) both side with the fallback, making the Prerequisites hard requirement the lone outlier. Refutation attempts failed: the fallback is unconditional (not scoped to non-default contexts), the gap state is real and documented (vid-voice-capture line 41 defers contexts on thin sources, and pipeline mode proceeds with no voice files), and the hard stop is not the right call given the rest of the system. Two refinements: (1) line 31's redirect is for "foundation" generally and never addresses missing reference-pieces, which worsens the finding since the nearest redirect (/foundation) cannot create these files per vid-voice-capture line 13; (2) in the exact fresh-creator scenario, voice-profile.md is also absent so the line 54 fallback cannot literally execute, meaning the contradiction bites most cleanly in the deferred-context and pipeline-mode states rather than the one the finding names. Severity stays moderate: the fallback path is safe and documented, so the bad outcome is a wrong block or run-to-run nondeterminism (rigidity/inconsistency), not broken output or fabrication.

### [T15] Credibility slot decision offers three slots but the procedure can only execute the problem-result slot
**Evidence:** SKILL.md line 139 says pick among "the three slots (Hook / Problem-Result / Setup)", and the packet (line 265) allows woven_into: hook | problem-result | setup | none. But the confirm step (line 149) is hardcoded to "Show the creator the credibility line in context (rewritten Problem/Result with credibility woven in)", Phase 3's Setup build (lines 151-165) has no credibility-weave step even though Setup is a legal slot (and the documented best fit for Effort signal per intro-architecture Step 6), and Phase 4 assembly (line 190) hardcodes "Problem/Result with credibility woven (locked in Phase 2)". A Hook-slot weave would also require rewriting a hook the creator already locked, with no step for re-approval.

**Outcome:** Two of the three advertised slots are unexecutable as written: a Setup weave has no draft to weave into at Phase 2 confirm time and no weave instruction at Phase 3, and a Hook weave silently mutates a locked component. In practice every intro gets funneled into the problem-result slot regardless of the form-to-slot fit, or the model improvises off-procedure.

**Skeptic:** Evidence verified at every cited line: SKILL.md:139 offers three slots, :265 packet allows all three plus none, yet :149 hardcodes the confirm to a rewritten Problem/Result, Phase 3 (151-183) has no weave step, and :190 hardcodes assembly as "Problem/Result with credibility woven (locked in Phase 2)". The timing contradiction is real: a Setup-slot weave decided in Phase 2 has no draft to confirm against (Setup is built in Phase 3), and Phase 4's stitch instruction contradicts any non-P/R slot decision. Refutation attempts partially land but do not kill it: (1) references/credibility-line-weaving.md contains full worked Hook and Setup weave examples plus a decision flow, so the slots are executable by a model that consults it, BUT that file is absent from the Phase 1 silent-load list (items 1-15) and only appears in the reference index at line 365, so the procedural spine remains problem-result-only; (2) "silently mutates a locked hook" is overstated since Hook-slot weaves normally arrive via the Credibility hook type and Phase 4's assembled-intro display plus read-aloud test means nothing ships unseen; (3) "every intro funneled into problem-result" overstates a bias that is partly the documented default ("Default to Problem/Result if unsure", weaving ref line 148); (4) the Effort-signal-to-Setup mapping is in credibility-line-weaving.md line 33, not intro-architecture Step 6 as the finding claims (minor misattribution). What survives is a genuine internal contradiction: two of three advertised slots require off-procedure improvisation, and line 190 literally instructs the wrong stitch for a Setup weave. Concrete outcome: default-slot bias and procedural drift (Effort-signal credibility landing in P/R when the form-to-slot logic says Setup), in a skill whose value is procedure fidelity. Not critical: no fabrication risk, no broken downstream contract (packet/piece.md fields support all slots), and creator review gates catch final output. Fits the moderate definition exactly (rigidity, drift risk); the auditor's severity label was honest even though the outcome narrative was inflated.

### [T3] Rewrite is an advertised entry ("rewrite the intro", "the opening feels off") but the procedure has no rewrite path and no graph cleanup
**Evidence:** SKILL.md line 19 lists "Creator wants to rewrite the intro because the current one feels off" as a run trigger, and the description (line 3) triggers on "rewrite the intro", "fix the intro", "the opening feels off". No phase ever reads the existing ## Intro or the piece's existing intro_* fields; Phases 1-5 always generate from scratch and Phase 5 (lines 225-233) writes over script.md ## Intro and piece.md. Nothing reconciles stories_used/proofs_used or bank used_in entries woven by the previous intro version. Contrast vid-structure SKILL.md line 23, which explicitly detects a prior outline and asks refine-or-rebuild.

**Outcome:** A creator saying "the opening feels off" gets the full 5-phase from-scratch interview instead of a targeted fix of the existing draft, and if the rewrite drops a previously woven story or proof, the piece's stories_used and the bank entry's used_in keep pointing at content that no longer exists in the script, permanently corrupting the wikilink graph the system calls non-negotiable.

**Skeptic:** All cited evidence verified against real lines. SKILL.md line 3 advertises "rewrite the intro" / "fix the intro" / "the opening feels off" as triggers and line 19 lists rewrite as a run reason, yet no phase implements a rewrite path: Prerequisites (lines 24-31) never check intro_locked, no phase detects an existing ## Intro or asks what feels off, and Phases 1-3 hard-gate on fresh approvals, so the creator gets the full 5-phase from-scratch interview. Phase 5 (lines 221-234) overwrites script.md ## Intro and the intro_* fields, and line 233's wikilink-graph instruction is purely additive ("If a story, proof, or testimonial got woven in, update both sides"), with no reconciliation of the prior version's stories_used/proofs_used or the bank entries' used_in backlinks. A rewrite that drops a woven story leaves stale bidirectional links in a system whose own rules call link integrity non-negotiable, and story-pulling-criteria's reuse hygiene then makes downstream selection decisions on false used_in data. The contrast is real: vid-structure SKILL.md line 23 explicitly detects a prior outline and asks refine-or-rebuild, so the expected re-run pattern exists in the sibling and is absent here. Minor caveats: line 60 does load script.md body so the old intro may be seen in passing (the "never reads" claim is slightly overstated), and "permanently corrupting" oversells recoverable staleness. Severity is correctly moderate, not critical: the skill still produces a valid intro, no fabrication risk, no unfulfillable downstream contract; this is drift risk and rigidity, which matches the moderate definition exactly.

### [cross-file-claims] Phase 4 visual-matching note claims a thumbnail style no upstream file produces
**Evidence:** SKILL.md:198 scripts: "Heads up: the thumbnail style is {cinematic / scrappy / studio / location}. The first SHOT of the video should match." The only loaded sources are thumbnail-brief.md and intro-architecture.md. vid-thumbnail/SKILL.md:3,10 declares itself "a TEXT planner only. It does NOT design the visual (no layouts, hero choices, expressions, color application...)"; its brief schema (vid-thumbnail/SKILL.md:147-164: title_paired, strategies_tested, picks, creation_path) and assets/thumbnail-brief-template.md contain no style field (grep for cinematic/scrappy/studio/location/style: zero hits). intro-architecture.md:219-231 supplies only the style-to-shot table, not the style itself.

**Outcome:** Every run of Phase 4 must fill the {cinematic/scrappy/studio/location} slot with data that exists nowhere in the loaded context, so the skill either invents the creator's thumbnail style in a creator-facing message (fabrication in a skill whose spec repeats anti-fabrication ten times) or emits a broken placeholder. The mandated visual-matching safeguard is fed by a phantom field.

**Skeptic:** Evidence verified line by line. SKILL.md:198 mandates the message "Heads up: the thumbnail style is {cinematic / scrappy / studio / location}..." and no loaded source supplies that value: vid-thumbnail is text-only by declaration (SKILL.md:130, 138) and its brief schema (lines 147-164: title_paired, strategies_tested, picks, creation_path) has no style field; intro-architecture.md:219-232 supplies only the style-to-shot table and says the creator/editor enforces the constraint; grep confirms the cinematic/scrappy/studio/location taxonomy appears in no file vid-intro loads. Refutation attempts fail and one strengthens the finding: the style DOES exist in the vault at foundation/packaging-system.md ("Thumbnail style (wrapping paper)" with a Style: field, per knowledge/packaging-system-template.md:19-22), but vid-intro contains zero references to packaging-system, so this is a real cross-file wiring miss with an obvious fix, not a pattern-matched rule. Severity stays moderate, not critical: the slot lives in a one-line creator-facing chat advisory, not in saved script content or any downstream contract field, so the worst outcome is a guessed style label the creator immediately sees and can correct, or an improvised question. No output corruption, no broken pipeline state. But it is above minor because the spec structurally invites a small fabrication in a skill whose spine is anti-fabrication, and the mandated visual-matching safeguard degrades to a guess or a genericism on every run until the load list or the message template is fixed.

### [cross-file-claims] Credibility-Hook risk flag reads channel size from creator-foundation.md, which has no channel-size data
**Evidence:** SKILL.md:90: "If `creator-foundation.md` shows a small or new channel AND a Credibility Hook is in the running, flag it." references/hook-type-selection-flow.md:21 names input 3 as "Channel size and creator credibility (from `foundation/creator-foundation.md`)" and its worked examples assume subscriber counts ("Channel is 8k subscribers" line 28, "Channel has 800 subscribers" line 44). The /foundation-owned template (knowledge/creator-foundation-template.md:15-23) defines exactly seven sections: Offer, Avatar, Top 3 perceived problems, Iceberg Statement, Content pillars, Credibility brags, Backstory. No channel size, subscriber count, or channel age anywhere.

**Outcome:** The documented small-channel safeguard (the one behavior distinguishing a risky Credibility Hook from a safe one) can never fire from its stated source. At runtime Claude either silently skips the check or guesses whether the creator's channel is "small or new" and asserts that guess to the creator, both wrong.

**Skeptic:** Evidence verified line by line. SKILL.md:90 sources the small-channel check to creator-foundation.md; references/hook-type-selection-flow.md:21 names "Channel size and creator credibility (from foundation/creator-foundation.md)" as input 3, with worked examples assuming subscriber counts (lines 28, 44, 88) and a mandatory silent step "Check channel size" (line 78). knowledge/creator-foundation-template.md:15-23 defines exactly seven sections (Offer, Avatar, Top 3 problems, Iceberg, Pillars, Credibility brags, Backstory) with no channel size, subscriber, or age field. I also checked the /foundation builder skills (plugins/authentic-ai-os/skills/vid-avatar, vid-credibility, vid-positioning, vid-backstory): none asks for or stores the creator's channel size; subscriber numbers appear only inside example brags/backstories. vid-research stores competitor sizes only. So the datum structurally does not exist at the stated source, and the reference models it as a first-class structured input anyway. The claimed outcome is slightly overstated ("can never fire": a backstory can incidentally leak a subscriber count, and Claude can simply ask the creator, which is what sibling vid-title does at references/title-filters.md:98 for the same rule without citing a phantom source), but the core cross-file contract mismatch is real and the predictable runtime behaviors are a silent skip of a documented safeguard or an unsourced guess about channel size. Not critical: it is a soft-friction advisory flag that only bites when a Credibility Hook is in the running, the creator decides anyway, and a wrong guess about the creator's own channel size self-corrects instantly in conversation; no fabrication reaches the script. That lands squarely in the moderate bucket (drift risk, contract resting on data the system never collects), matching the auditor's grade.

### [cross-file-claims] Prerequisites hard-require reference-pieces, contradicting the load contract the skill itself cites and its own Phase 1 fallback
**Evidence:** SKILL.md:26 lists under "Hard requirements": "`foundation/reference-pieces/` has at least the `youtube-script` context (the voice engine)". But SKILL.md:54 (Phase 1 load 6) says "If absent, seed from the guardrail fingerprint and note the gap", and the authority contract vid-intro points at (knowledge/voice-profile-schema.md:112, load contract step 4) mandates the same graceful fallback: "If that file does not exist, seed from the `voice_fingerprint` and `signature_phrases` only, and note the gap." voice-pressure-test.md:44 likewise defines skipped-Pass-2 behavior for a missing file.

**Outcome:** Two contradictory behaviors for the same missing file inside one skill: a standalone run that obeys Prerequisites hard-stops a creator whom the voice contract (and the skill's own load step, and the pressure-test schema's skipped-no-reference value) explicitly supports. Which one the creator gets depends on which paragraph the model weights that session. Distinct from the already-reported vid-pipeline never-a-blocker conflict: this contradiction is internal to vid-intro versus the voice-profile-schema contract it claims to follow.

**Skeptic:** Every cited line checks out. SKILL.md:24-26 hard-requires foundation/reference-pieces/ to have the youtube-script context; SKILL.md:54 (load step 6) says "If absent, seed from the guardrail fingerprint and note the gap" with no default-context carve-out; voice-profile-schema.md:105-112 (the contract SKILL.md:53 explicitly points at, self-declared "the single source" that "every writing skill follows exactly") mandates the same fallback; voice-pressure-test.md:44 and vid-intro's own output packet (SKILL.md:276, pass2_grain: skipped-no-reference) both treat the missing file as a supported state. Strongest refutation: a two-tier reading where Prerequisites guarantee the default engine and step 6's fallback only serves non-default voice_contexts. That reading is plausible and possibly intentional (vid-segment SKILL.md:31 repeats the identical hard requirement), but the text never states it, step 6's fallback plainly covers the default case, SKILL.md:31's remediation paragraph gives no instruction for the voice requirement failing, and vid-ending (SKILL.md:31) falls back even for a missing voice-profile.md, so the sibling set is inconsistent too. Result: a standalone run with the guardrail present but youtube-script reference absent gets either a hard stop or a schema-sanctioned fallback depending on which paragraph the model weights, and the hard requirement is an undeclared deviation from the canonical load contract. The finding is correctly scoped apart from the excluded vid-pipeline "never a blocker" conflict. Severity: not critical (neither branch yields bad output or fabrication; one is a stop with an obvious fix, the other is the contract-approved degraded mode), not minor (two contradictory instructions for the same file plus contract drift is more than polish). Moderate per the "rigidity, drift risk" definition; the auditor did not inflate.

## Minor (8)
### [T4] Four of the twelve unconditional Phase 1 loads are single-step context that should load lazily
**Evidence:** SKILL.md lines 47-63 list 12 unconditional "Silent loads" before the first creator message. banks/hook-bank.md (load 7) is consumed only at Phase 2 line 96; banks/transition-bank.md (load 8) only at Phase 3 line 167; knowledge/voice-pressure-test.md (load 3) only at Phase 4 line 202; knowledge/parable-decision-matrix.md (load 9) only for the Phase 2 energy-lane pick, with line 57 itself conceding it is "Body-segment scoped in name".

**Outcome:** The entire multi-file context lands before the Top 3 questions conversation even starts, degrading instruction-following across a five-phase creator conversation and hiding the real dependency order (a maintainer cannot tell which load feeds which step). Compare vid-structure's per-phase load table and vid-pressure-test's explicit "Deferred load" note; vid-intro is the outlier.

**Skeptic:** Evidence is fully accurate: SKILL.md lines 47-63 list 12 unconditional silent loads; hook-bank (line 55) is first used at line 96 (Phase 2), transition-bank (line 56) at line 167 (Phase 3), voice-pressure-test (line 51) at line 202 (Phase 4), and line 57 concedes parable-decision-matrix is "Body-segment scoped in name". The sibling contrast is real: vid-structure SKILL.md has a per-phase load table (line 25) with the rule "Never load all the banks up front" (line 37), and vid-pressure-test SKILL.md line 59 explicitly defers its single-consumer load. vid-intro is the outlier against the house pattern. However, the claimed outcome is inflated: all four cited files are consumed in EVERY complete run (Phase 2 always pulls hook-bank, Phase 3 always pulls transition-bank, Phase 4 always runs the pressure test), so deferral changes only timing, not total context; no load is wasted, unlike the truly conditional/on-demand cases in the siblings. The "degrading instruction-following" harm is speculative (slightly heavier context during Phase 1 only), and dependency order is not really hidden since each load line carries a purpose annotation and the reference index restates purposes. What remains is a real pattern-consistency and load-scheduling cleanup, which fits "works fine, polish only" (minor) rather than moderate.

### [T8] Output packet key proof_used (singular) mismatches proofs_used everywhere else
**Evidence:** SKILL.md:267 "proof_used:" in the intro_packet vs SKILL.md:233 and vault-integration.md:403 "proofs_used". The packet also uses stories_used (plural, line 269), so the singular is an inconsistency within one YAML block.

**Outcome:** Any future packet consumer keying on proofs_used silently misses the proof list. Harmless today only because the packet has no consumer (see T6 finding).

**Skeptic:** Evidence verified exactly as cited: SKILL.md:267 has `proof_used:` (singular) inside the intro_packet while the same block uses `stories_used` (plural, line 269), the Phase 5 piece.md write uses `proofs_used:` (line 233), and the authoritative schema in knowledge/vault-integration.md:403 (and :712) is `proofs_used`. The finding's outcome framing is partly wrong: the packet DOES have a consumer (vid-ending SKILL.md lines 62-66 and 262 read setup.text, hook.type, etc. by name), but that consumer never reads the proof field, and consumers are LLMs, not parsers, so nothing "silently misses" anything today; the canonical proof record in piece.md is written with the correct plural anyway. What keeps this real rather than false-positive: the packet is an explicitly documented cross-skill contract ("team-standard packet shape," mirrored in vid-ending line 197), and the singular typo has already propagated verbatim into documents/audits/flow-review-2026-05-08.md line 113, demonstrating the drift cost is not hypothetical. It is a genuine one-word naming inconsistency in a shared contract. Severity minor is honestly graded: works fine, polish-only fix.

### [T9] Problem/Result annotations leak internal scoring labels into the creator-facing list
**Evidence:** .claude/skills/vid-intro/SKILL.md lines 122-123 annotate candidates with "(Top 3 problem #1)"; references/problem-result-options.md lines 125-126 go further: "(Top 3 problem #1, pain-intensity high)" and "(Top 3 problem #1, both halves anchored)", immediately after line 113 says the decision short-list runs "silently in Phase 2".

**Outcome:** The pain-intensity scoring rubric is exactly the elimination process hook-type-selection-flow.md line 82 says never to show. The creator sees Claude's internal grading vocabulary ("pain-intensity high", "both halves anchored") attached to lines they are supposed to judge by ear, nudging them to evaluate the machinery instead of the words. Low blast radius since "Top 3 problem" is at least foundation vocabulary the creator knows.

**Skeptic:** Evidence verified at every cited line. SKILL.md lines 122-123 annotate with "(Top 3 problem #1)" (harmless, foundation vocabulary). The real bite is inside references/problem-result-options.md: line 113 declares the decision short-list runs "silently in Phase 2", line 115 defines the "pain intensity" score, then the example at lines 125-126 surfaces that score to the creator as "(Top 3 problem #1, pain-intensity high)" and "(Top 3 problem #1, both halves anchored)". That is a self-contradiction within one file: the template instructs Claude to reproduce subjective grading jargon in creator-facing output that the section header says stays silent. The citation of hook-type-selection-flow.md line 82 ("Don't show them the elimination process. That's noise.") is a weaker cross-file analogy (it governs hook lane elimination, and the same SKILL.md example deliberately annotates hooks with "(Q-1, 14 words, ~4s)"), but those hook annotations are verifiable candidate facts, not internal grades, so the analogy does not kill the finding. Attempted refutation "annotation is by design" fails because the design intent (line 121 "with annotation") does not license leaking the silent scoring rubric, only labeling the candidate. Concrete outcome: creator-facing lists carry machine grading vocabulary the skill's own principle labels noise. Nothing breaks, no fabrication risk, no downstream contract harmed, and the leak lives only in a reference example. Minor is the honest grade; the auditor did not inflate.

### [T15] Line-8 phase summary contradicts the actual 5 phases: omits Phase 4, splits Phase 1, mislocates the credibility decision
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 8: "Five phases: load context, derive viewer questions and pick hook type, build Hook plus Problem/Result, build Setup plus Transition with credibility woven, save." Actual phases: Phase 1 = load + derive questions + pick hook (line 45), Phase 2 = Hook + Problem/Result AND the credibility weave decision (lines 94, 139-149), Phase 3 = Setup + Transition (line 151), Phase 4 = assemble + pressure-test (line 185), Phase 5 = save (line 221).

**Outcome:** The summary's five items map to phases 1,1,2,3,5: the assemble/voice-pressure-test/read-aloud phase is missing entirely, and credibility weaving is placed in the Setup/Transition step when the instructions for it live in Phase 2. A model or maintainer orienting from the summary skips the only quality gate in the skill or looks for weave instructions in a phase that has none.

**Skeptic:** Evidence verified real: SKILL.md line 8's summary genuinely omits Phase 4 (line 185, assemble + pressure-test), merges/splits Phase 1 (line 45), and attaches "credibility woven" to Setup/Transition when the weave decision lives in Phase 2 (lines 139-149; Phase 3 at line 151 has no credibility content). But the claimed outcome (model skips the quality gate or hunts for weave instructions in Phase 3) does not follow: line 8 is a non-executable orientation sentence, the walkthrough at line 41 has five explicit headed phases that drive execution, no routing surface (frontmatter description line 3, vid-pipeline) consumes line 8, and Phase 2's explicit "Decide where credibility gets woven" instruction overrides any priming. Residual harm is maintainer-drift confusion from a body-internal contradiction, fixable by rewriting one sentence. Real finding, but severity inflated from moderate; correct grade is minor (works fine, polish only).

### [T15] Off-by-one credibility slot numbering: 'woven into one of 1-3' contradicts the skill's own architecture and output packet
**Evidence:** c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md line 10: "steps 1-5 of the 6-part architecture, with credibility woven into one of 1-3". Under the skill's numbering (knowledge/intro-architecture.md lines 27-38: step 1 = TOP 3 VIEWER QUESTIONS, 2 = HOOK, 3 = PROBLEM/RESULT, 4 = SETUP), steps 1-3 exclude Setup. But SKILL.md line 139 ("the three slots (Hook / Problem-Result / Setup)"), packet line 265 ("woven_into: hook | problem-result | setup | none"), and references/credibility-line-weaving.md line 17 all include Setup as a valid slot. Canon numbering (Hook=1, P/R=2, Setup=3) is where '1-3' came from.

**Outcome:** The scope statement and the working spec disagree about which beats can carry credibility. A model resolving 'one of 1-3' literally against the architecture either rules out Setup weaves (breaking the Effort-signal-in-Setup pattern the reference file teaches at length) or tries to weave credibility into the Top 3 questions, which are never spoken. Same off-by-one is baked into intro-architecture.md line 25 vs its own line 38, so the contradiction is spec-level, not a typo in one place.

**Skeptic:** Evidence fully verified: SKILL.md line 10 ("steps 1-5 of the 6-part architecture, with credibility woven into one of 1-3") and intro-architecture.md line 25 both use the "one of 1-3" shorthand, which under the architecture's own numbering (1=Top 3 viewer questions, 2=Hook, 3=Problem/Result, 4=Setup) excludes Setup and includes the never-spoken viewer questions. This contradicts intro-architecture.md line 38, SKILL.md lines 139 and 265, and the entire credibility-line-weaving.md reference (line 17 and worked Setup-weave examples). So the finding is real, and it is a spec-level inconsistency in two files, likely inherited from canon numbering where Hook=1. However, the claimed bad outcome does not follow in practice. The slot decision is made in Phase 2 against SKILL.md line 139, which explicitly names Hook/Problem-Result/Setup; the packet enum includes setup; the dedicated runtime reference the skill loads for exactly this decision teaches Setup weaves at length with a decision flow that includes Setup; and in intro-architecture.md the wrong shorthand at line 25 is immediately followed by the correct expansion at line 38 and again in Step 6's own section. SKILL.md line 8 even says Phase 3 is "build Setup plus Transition with credibility woven." An ambiguous two-word shorthand in a scope-boundary sentence loses to five unambiguous, colocated statements at every point where the decision is actually executed. Neither failure mode (ruling out Setup weaves, weaving into Top 3 questions) is realistic. The cost is consistency/polish: fix "one of 1-3" to "the Hook, Problem/Result, or Setup" in both files. That is minor (works fine, polish only), not moderate; the drift-risk claim is inflated.

### [T2] vid-intro never reads vid-structure's locked outline; the Setup contract and transition checks are aimed at the brain dump instead
**Evidence:** SKILL.md line 58 loads piece.md for "(format, goal, pillar, locked title)" only, never segment_purposes or tension_plan (which vid-structure writes per its SKILL.md lines 14-15). Line 183 validates the transition against "the brain dump's first body point" and craft note 2 (line 332) checks the Setup against "whether the brain dump actually delivers all three", but the brain dump is unordered raw material with cut tangents; the actual first body point and the payoff-late ordering live in script.md / tension_plan (vid-structure SKILL.md line 83: title's answer deliberately pushed past the midpoint).

**Outcome:** In pipeline order (structure then intro) the intro can promise material structure cut, and the transition can forward-hook straight into the title payoff that the tension plan deliberately deferred, leaking the payoff in second 25 and breaking the retention plan the previous skill just built. The check as written validates against the wrong artifact so it cannot catch either.

**Skeptic:** Evidence lines are real (SKILL.md 58, 183, 332; vid-structure 14-15, 83/94 verified), but the headline claim "vid-intro never reads vid-structure's locked outline" is refuted by the skill itself: load item 12 (SKILL.md line 60) explicitly loads "script.md body" and names it "the actual material the intro must align to"; line 285 accepts "the script body" as a claim source; and piece.md, which carries tension_plan, is loaded whole at line 58 (the parenthetical omits the field, the file is not omitted). Line 20 confirms pipeline mode runs after structure, so script.md exists and is loaded. The claimed payoff-leak outcome does not follow: the transition contract (line 168) anchors to "the first body point", which structure deliberately made a non-payoff point, and that point is visible in the loaded script.md; leaking the deferred title answer requires ignoring both the load list and the transition anchor. The cut-material outcome is an edge case since Setup promises derive from the same locked title structure ordered its points to pay off. What survives is genuine but small: line 183 says "the brain dump's first body point" (a category error, brain dumps are unordered; the first body point lives in script.md) and line 332 checks Setup delivery against the brain dump where the script body is authoritative post-structure. Real wording drift from the vid-structure rewrite, fixable with two phrase edits, but the correct artifact is already loaded and designated the alignment target, so this is polish, not the moderate drift-with-broken-retention-plan claimed. Downgrade to minor.

### [T3] Standalone mode never resolves which piece to work on
**Evidence:** SKILL.md line 3 triggers on slug-free phrases ("I need a hook", "the opening feels off"), line 35 defines standalone mode, and line 39 covers only the caller-supplied-slug case ("If invoked with context from a caller... skip the load step's questions"). No step asks or derives {slug} in standalone mode; Phase 1 (lines 58-60) immediately loads content/pieces/{slug}/* files. Siblings script this: vid-thumbnail SKILL.md line 37 hard-requires "the video slug argument, or an existing .../piece.md"; vid-structure line 23 implies a "which piece?" question.

**Outcome:** With several in-progress pieces, a slug-free invocation forces the model to improvise piece resolution, either scanning content/pieces/ (violating the vault CLAUDE.md "ASK before scanning" rule) or guessing, with the worst case being an intro written into the wrong piece's script.md.

**Skeptic:** All cited evidence verified line by line. SKILL.md line 3 triggers on slug-free phrases, line 39 covers only the caller-context path, and Phase 1 (lines 58-60) loads content/pieces/{slug}/* with no scripted slug resolution anywhere in SKILL.md or the three references files. Siblings prove the house pattern: vid-thumbnail line 37 hard-requires the slug argument, vid-structure line 23 scripts a "which piece?" question for standalone. Line 39's phrasing "skip the load step's questions to creator" even implies standalone questions were intended but the which-piece question was never written, so this is a real omission, not a deliberate choice. The auditor's worst-case outcome (intro written into the wrong piece's script.md) is overstated because Phase 1 surfaces the locked title in the Top 3 viewer questions message and the skill gates on multiple creator approvals before any save, so misresolution gets caught early. But the milder concrete outcome holds: slug-free invocation forces improvised piece resolution, and directory-listing content/pieces/ directly violates the vault CLAUDE.md rule against pre-scanning content pieces. Minor is the honest grade: works in practice (models default to asking, and creator approvals backstop it), one-line fix to match siblings. Not inflated, not false.

### [cross-file-claims] Reference index mislabels story-pulling criterion 2 as "problem match"; the file renamed it "Point match" and dropped problem-number matching
**Evidence:** SKILL.md:355: "5 criteria for picking the right story from N candidates (stage match, problem match, outcome specificity, type match, reuse hygiene)". knowledge/story-pulling-criteria.md:45 names Criterion 2 "Point match" and explicitly rejects the old framing: "Match the story's `illustrates` line and `themes` to the pain in the segment's emotional opening, by meaning, not by a fixed problem number."

**Outcome:** Stale label re-imports the retired fixed-problem-category matching model into the always-loaded SKILL.md; a model following the index wording filters credibility-line story candidates by Top-3-problem number instead of by meaning, which the criteria file was rewritten to prevent. Low blast radius (conditional load, file itself corrects it when read).

**Skeptic:** Evidence verified exactly: SKILL.md:355 says "problem match" while knowledge/story-pulling-criteria.md:43-45 names Criterion 2 "Point match" and explicitly rejects matching "by a fixed problem number" (line 59 adds "Reuse, not buckets"). This is real drift, not intentional shorthand; the same retired term also survives in vid-segment/assets/segment-scaffold-template.md:21, proving the rename was not propagated. However, the claimed behavioral outcome is weaker than stated: SKILL.md:61 mandates loading the criteria file whenever the story-bank loads, so the self-correcting path is part of the normal flow, and the Top-3-problem-number misfiltering scenario requires the model to skip that mandated load. The defensible concrete outcome is a stale always-loaded index label that misdescribes a rewritten shared criterion (maintainability/drift cost, mild re-import risk given "Top 3 problems" is advertised three rows down at line 358). Auditor's minor grade is honest and correct; confirmed as-is.

## Dismissed as false positives (16)
### [T6] The sub-skill output packet has no consumer anywhere in the shipped system
The central claim ("intro_packet appears nowhere outside vid-intro"; "no consumer anywhere in the shipped system") is factually wrong. vid-ending SKILL.md consumes the packet by name: Phase 1 load step 14 (lines 62-67) reads setup.text, setup.top_3_questions_used, problem_result.top_3_problem_anchored, hook.text, hook.type, and credibility.text from "the intro packet returned by vid-intro to the caller"; the callback rules (lines 98-102) instruct reading those exact fields; and the reference index (line 262) cites "Schema in vid-intro SKILL.md 'Output packet' section." The very field the finding claims dies with the turn (top_3_questions_used) is one vid-ending explicitly reads to pay off the Setup contract. The auditor checked vid-pipeline (which routes on piece.md, true but irrelevant since the packet is for downstream skills, not routing) and vid-segment (a sibling that takes its own caller packet), and missed the real consumer. The claimed outcome, a "false contract" misleading maintainers that downstream context flows through the packet, does not follow: downstream context genuinely flows through it, with script.md's ## Intro section as the documented standalone fallback (vid-ending line 261). Residual truth is only that two annotation fields (hook.pattern_id, transition.pattern_id) have no named reader, which is minor polish and not the finding as stated.

### [T6] intro_strategy, intro_hook_type, intro_credibility_form are written to piece.md but nothing shipped reads them
Evidence is factually accurate (SKILL.md:228-230 writes the enums; vid-pipeline SKILL.md:88-89 routes on intro_locked only; repo-wide grep confirms no shipped machine consumer of the three enum values). But the claimed outcome does not follow. (1) The finding's core assertion, that the contract "accretes unverified" and should live with a future consumer, is refuted by knowledge/vault-integration.md:408, the designated frontmatter authority, which explicitly declares "vid-intro adds intro_locked + the intro_* fields." Writing them is compliance with the shipped schema contract, not accretion. (2) The "drift surface" cost is near nil: the enum values are single-sourced in vid-intro; vault-integration.md names the fields without duplicating enums, and the pressure-test asset (145-147) shows example values, not a second definition, so there is no copy to drift. (3) "Nothing reads them" ignores the system's design: an Obsidian vault where root CLAUDE.md mandates frontmatter on every note and immediate saving of decisions. The locked hook type/strategy/credibility form is the decision record the creator reads and can query via Dataview, and the pressure-test worked example already treats these fields as canonical piece.md state. No concrete bad outcome can be named, which is the audit's own bar for a real finding. Rule pattern-matched ("write-only metadata is dead") where it does not bite; the "violation" is the sanctioned design.

### [T9] Scripted candidate lists surface internal bank pattern ids (Q-1, HF-4) to the creator
Evidence lines are all real (SKILL.md 116-124, 173, 175, 178-181, 194; hook-type-selection-flow.md 82), but the core claim, that the skill violates its own invisible-machinery rule, fails on scope. Line 194's "No annotations, no labels, no meta" governs the Phase 4 ASSEMBLED intro (the clean read-aloud block), not Phase 2-3 candidate lists, which are deliberately annotated (word count, spoken seconds, soft-friction flags per line 315) to help the creator pick. hook-type-selection-flow.md line 82 governs the Phase 1 hook-lane elimination process, not candidate annotations. Annotated candidates then clean final block is a coherent two-stage design, not a contradiction. The finding's strongest punch (Q-1/HF-4 point at bank files nothing creates, so the ids are dead references) is exactly known gap (d), explicitly excluded from re-reporting; stripped of it, the ids are labels from creator-owned vault files (banks/ is creator-facing, like proof-bank) that trace to the output packet's pattern_id fields (lines 249, 262), i.e. traceability, not leaked machinery. Line 173's B-ids are an instruction to Claude about which phrases are Tier 2 ("surface with the failure mechanism explained"), not a mandate to recite ids. The claimed outcome ("reads as robot output") is an aesthetic judgment against an intentional design; no concrete bad outcome survives deduplication. At most, removing the two-character ids from the example templates would be polish.

### [T10] Roughly a dozen verbatim chat messages are scripted, about 10 hard-scripted, in a voice-sensitive skill
Evidence verified: all cited lines exist in .claude/skills/vid-intro/SKILL.md (71-76, 90, 116-124, 128, 183, 196, 198, 209, 214-215, 234, 287) and references/hook-type-selection-flow.md (46, 92); the count of roughly a dozen scripted messages is accurate. But the claimed bad outcome does not follow. (1) The voice-sensitive surface is the intro text the creator speaks on camera; none of the scripted lines touch it. They are Claude's process gates (questions, flags, save confirmations), and identical gate phrasing does not harm the authenticity premise. (2) The claimed clash with line 370 ('Conversation, not document. Short messages.') is inverted: the scripted lines ARE the short messages and enforce that principle. (3) Several scripted lines are load-bearing: line 90 and flow.md line 46 carry the credibility-risk rationale that must reach the creator, line 287 is the exact anti-fabrication kickback, line 215 is the read-aloud test the skill calls 'the final arbiter' (line 340). Scripting them guarantees critical checkpoints fire with their reasoning intact. (4) Templates are explicitly adaptable: 'Example shape' at line 114, {Q1}/{format} placeholders, situational '~45 seconds'; nothing mandates verbatim delivery, and models fill/adapt such templates. (5) The author wrote the lines in the product's own register ('Look right? Or want me to redraft,' 'Cool to proceed?'); in a repo whose brand is anti-AI-tell phrasing, hand-scripting Claude's gate lines to state intent-plus-register is a defensible deliberate choice, and replacing them with 'state intent, let the model phrase it' risks exactly the generic drift the product fights. (6) The only concrete harm cited (line 234 hardcoding 'Voice pressure test: pass' against the pass|soft-warn|soft-reject schema at line 273) is, by the finding's own admission, the separate T9 finding. T10's residual contribution is aesthetic sameness in chat UX with no nameable concrete bad outcome. A stylistic rule pattern-matched where scripting is the right call in context: false-positive.

### [T13] Top 3 viewer questions promoted from canon derivation tactic to Part 1 of the '6-part' intro
Refuted by the primary source. The auditor compared the skill against the extracted notes summary (notes.md line 21), which calls the structure "6-Part" while listing only 4 beats, and concluded the skill invented part 1. But Ed's actual lesson transcript (lesson-03-video-intros-101\transcript.md) explicitly numbers the steps: line 52 "step one of every intro you make is to simply ask yourself, what are the top three questions...", line 64 "step two of the intro is... the hook", line 118 "STEP 3... PROBLEM/RESULT", line 158 Setup uses "the top two to three viewer questions you worked out in step one", line 170 "step five... a transition", line 204 "step six" (credibility). The skill's numbering (SKILL.md line 3; knowledge\intro-architecture.md lines 23-40) matches Ed's original exactly; the cited canon notes file is the lossy artifact that cannot even account for its own "6-Part" name. "A part that never appears in the spoken intro" is how Ed himself teaches step 1, so Phase 4 stitching 4 spoken beats (SKILL.md lines 187-193) is faithful, not contradictory. The only real defect in the vicinity, SKILL.md line 10 saying credibility weaves "into one of 1-3" when the slots (Hook/Problem-Result/Setup) are steps 2-4, is a genuine indexing slip but belongs to the separately reported T15 finding, and its cause is not a "renumbering" since the numbering is canon-faithful; the operational instruction at SKILL.md line 139 names the slots correctly, so behavior is unaffected.

### [T13] 5 credibility forms where canon names 4
Evidence verified as accurate: canon (business-os/Resources/references/ed-lawrence-ygs/frameworks.md, Credibility Line section) names 4 forms; vid-intro SKILL.md, both enums (intro_credibility_form and the output packet), and knowledge/intro-architecture.md consistently carry 5. However, the divergence is a deliberate, internally consistent refinement, not drift: Ed's 'big result' was split into big-personal-result and big-client-result, with a purpose-written example for the new form, and the split is propagated identically through every consuming surface. Category labels carry no fabrication risk (credibility content is still pulled from creator-foundation.md under anti-fabrication rules), nothing breaks at runtime (auditor concedes this), and no shipped file attributes the 5-form list to Ed, so there is no mismatch against an attributed claim. The only stated bad outcome, 'future audits will flag a phantom fifth form,' is circular audit meta-noise, not a concrete harm to the skill or creator. Extending a source taxonomy in your own derivative system is the right call in context. A genuinely nearby issue exists but is a DIFFERENT finding: Ed's four verbatim examples (500 cakes, $90,000, 40 people, 500 resumes) sit in shipped knowledge/intro-architecture.md with no attribution despite canon's attribution-required warning. That is not what this finding claims.

### [T14] Anti-fabrication rule stated 10 times in one file
Evidence is real (all ten cited lines exist in c:\Users\billr\projects\authentic-ai-os\.claude\skills\vid-intro\SKILL.md at lines 3, 43, 65, 96, 101, 134, 200, 283-289, 295, 371), but the claimed outcome does not follow. The structure is hub-and-spoke, not ten peer copies: line 65 defines the lock list once, and lines 96, 101, and 295 reference it by name ("from the lock list", "Anything not in the lock list") rather than duplicating the definition, so a refinement edits one place, not ten. The finding's drift mechanism is factually wrong. Lines 283-289 being the sole home of the rationale and kickback script is centralization working as intended, not divergence. The remaining instances are distinct functions at distinct fabrication risk points: trigger description (3), candidate-generation application at the highest-risk moment (96, 101), enforcement on a bad creator pick (134), extension to the visual-proof domain (200), auto-reject gate (295). Fabrication is this skill set's critical failure class per the audit's own severity rubric; restating the load-bearing constraint at each point of application in a 388-line LLM-executed workflow is deliberate reinforcement, and trimming it would raise the risk the rubric grades critical. Token cost is roughly 15 lines, most of which carry other necessary content. At most two sentences (line 43 clause, line 371 principle) are pure restatement, which is polish, not the moderate bloat/drift finding filed. Per the rubric, a violation that is the right call in context is a false positive.

### [T14] 'Hooking, not teaching' stated 4 times
Evidence is real (all six cited lines verified: SKILL.md 309, 310, 330, 375; intro-architecture.md 241, 302) but the claimed outcome does not follow. (1) "Over-block" is impossible by the file's own structure: hard rejects are a closed enumerated list (SKILL.md 295-300) that does not include teaching; teaching-in-intro lives only under "Soft friction (flag and explain, creator decides)" with the override instruction inline (lines 302-310). The Principles section is a closing ethos summary, not an enforcement mechanism, so no model has to "reconcile" which rule governs candidate handling. (2) "Ignore the friction flag" is equally unsupported: line 315 gives the exact annotation format for surfacing soft flags. (3) The alleged strictness conflict is not a contradiction: "contextual setup that LOOKS like teaching can land" is a classification note (setup resembling teaching is not teaching), fully compatible with "save teaching for the body." intro-architecture.md line 298 even states the reconciliation explicitly ("Defaults... aren't laws"). (4) The repetition is deliberate altitude layering (mechanical check, internal craft note, summary principle) of what the knowledge file names "the single most important principle" (line 300), which is the right call for an LLM-executed skill. Residual maintenance cost (four spots to edit) was not the claimed outcome and is negligible with the knowledge file as canon. This is a rule ("don't repeat yourself") pattern-matched where it does not bite.

### [T14] Read-aloud test stated 4 times
Evidence lines are real (103, 213-217, 340, 376 in .claude\skills\vid-intro\SKILL.md) but the characterization "four statements of one gate" is wrong. Line 103 is a Phase 2 candidate-generation filter (Claude-internal, pre-creator), not a repeat of the Phase 4 creator gate; calling it decorative is factually incorrect. Line 340 is judgment rationale in an explicitly internal craft-notes section; line 376 is one bullet in a Principles recap section where every bullet echoes earlier text by design. The only operative procedure lives solely at 213-217, so there is no duplicated parameterized content that could drift. Repetition of the system's core voice test matches the vault CLAUDE.md rule 8 ("Read aloud is the voice test... applies anywhere their voice appears") and is deliberate reinforcement, standard for long skill prompts. Token cost of the echoes is ~60 words in a 388-line file. No concrete bad outcome can be named; removing line 103 would actually worsen behavior.

### [T14] 'Conversation, not document' stated 3 times
Evidence quotes are real (SKILL.md lines 43, 47, 370) but the finding does not hold. (1) The count is inflated: line 47 "Silent loads (do NOT paste into chat)" is a point-of-use label on the 15-item load list, the exact spot where a dump would occur, not a restatement. (2) The remaining pair (walkthrough-top statement + Principles recap bullet) is deliberate house style across the whole skill family: vid-title (43/172ff), vid-segment (54/235), vid-ending (43/233), vid-thumbnail (14/180) all carry the identical pattern. vid-intro conforms; deleting the recap would make it the inconsistent sibling. (3) Repeating the hardest conversational constraint early and late in a 388-line prompt file is a standard LLM-reliability technique (attention decay over long context); the redundancy is the right call, not bloat. (4) No concrete bad outcome exists: cost is one bullet line, no behavioral divergence between the phrasings, no drift that could change output. "Pads the file" is not an outcome under the audit's own severity bar.

### [T14] Format-planner-first stated 4 times
Evidence lines are real but the finding conflates three distinct statements as one repeated rule. SKILL.md line 50 governs intro SHAPE (length/compression, confirmed by line 196), not hook precedence. Lines 338 and 373 are load instructions ("read the format planner before generating") that never mention voice or conflict resolution. Only line 80 and references/hook-type-selection-flow.md address format-vs-voice precedence, and they agree: line 80 says the planner sets which lanes fit, then "Voice profile's preferred_hook_types weights which lanes... Cross-reference both," which is exactly the reference file's four-input flow (lines 17-24) with its explicit override cases (lines 84-92). "Dictates the fit set" vs "usually wins conflicts within it" is compatible, not drift; runtime behavior is identical under either phrasing. Repetition of core principles at point of use is this skill's deliberate house style (anti-fabrication repeats at lines 43, 65, 96, 134, 200, 283-289, 295, 371, far more than format-first) and the reference index (lines 364-366) shows duplication is actively managed via "Does NOT restate" contracts. The claimed outcome (five-location lockstep edit if precedence changes) is hypothetical maintenance cost on locations that mostly do not state the precedence, with no bad output, fabrication risk, or contract break today. Reinforcing a load-bearing rule where it is used is the right call in a long skill prompt.

### [T15] No skip path for Problem/Result, Setup, or credibility although loaded planners default to skipping them and the skill's own enums define none
Evidence lines are all real, but the finding's mechanism is refuted. Its load-bearing claim ("skip logic lives only in references/credibility-line-weaving.md, never loaded at runtime") is false and self-contradicting: the skip logic is in two MANDATORY Phase 1 silent loads, knowledge/format-planners/{format}.md (SKILL.md:50, "This dictates the intro shape for THIS video"; news.md:43-46 "Often skipped") and knowledge/intro-architecture.md (format map: News = "NO Setup, NO Credibility line"), both of which the finding itself cites as evidence. The "none" enums (SKILL.md:230, :265) are therefore reachable through the documented path: the skill grants the planner trimming authority four times (SKILL.md:50, :157 "the format planner may say fewer is fine", :196 "News compresses to 5-10s", :338 "each format trims... The format planner is the adaptation layer... Don't force a Deep Dive's full 30-second intro onto a News video", plus principle :373). So deviation from the generic numbered steps is not "silent"; it is the skill's explicitly stated adaptation rule. The worst-case outcome ("format-wrong four-beat intro") also does not follow: news.md:43 allows a 1-line poke ("a creator with a strong POV can still poke or tease"), news.md:50 says defaults "are not gates. The skill should propose them as starting points", every beat passes a creator approval gate where skipping is trivial, and Phase 4's length check (SKILL.md:196) explicitly flags a long News intro. What survives is polish only: an explicit "if the planner skips this beat, record none" branch in Phases 2-3 would be tidier, but its absence causes no bad output given the loaded, authoritative trimming instructions plus per-beat gates. Classic pattern match of "enum with no producing step" that ignores that the producing step is the loaded format planner the skill defers to by name.

### [T2] Banned-transition tiers contradict the always-loaded intro-architecture, and the skill's source-explicit claim for B-6 is false
Both prongs of the finding collapse under verification. (1) The "false source-explicit claim" prong: "source-explicit" is a repo-defined term (documents\audits\ed-audit-flexibility-2026-05-08.md lines 46-49) meaning explicit in the Ed Lawrence source lessons, not in intro-architecture.md. B-6 "and finally / lastly" IS source-explicit: canon lesson-11-ending-your-video\notes.md line 40 says "Don't say 'and finally.'", and vid-ending\SKILL.md line 120 cites that exact source. It is absent from intro-architecture's table because that table covers intro-step-5 transitions while the "and finally" ban comes from the endings lesson; it correctly lives in the shared transition-bank Section 4 Tier 1 (transition-bank-template.md line 90), which is what SKILL.md cites. (2) The "contradictory enforcement" prong: intro-architecture.md does not call "Without further ado" or "Now, before we begin" hard rejects. Its anti-patterns section (lines 280-282, 298) explicitly says the entire friction list, hard-friction bullets included, is "Not auto-reject... flag them and explain why... The creator can override," which is operationally identical to the skill's Tier 2 handling (SKILL.md line 173: surface with failure mechanism, creator decides). Whichever loaded document the model follows, B-4/B-5 receive the same runtime treatment, so the claimed bad outcome (hard reject vs surfaced option divergence) cannot occur. The tier split is the deliberate, audit-documented design, not drift. Residual kernel is polish only: intro-architecture's table header "Banned" could annotate the tier split, but no concrete bad output traces to it given the doc's own creator-decides instruction.

### [T15] Deep Dive 5-thing Setup contradicts the mandate that every clause map to the Top 3 questions
Cited lines are real (SKILL.md lines 157, 163, 307, 69-78) but the finding's load-bearing claim, that "there is no instruction for where questions 4-5 would come from," is factually wrong. knowledge/format-planners/deep-dive.md line 50 ("For 45+ minute videos, set up 5 things instead of 3") and line 63 ("Viewer's top 3 questions (or 5 for 45+ min). Based on title and thumbnail.") supply exactly that instruction, and this planner is mandatory silent load #2 in Phase 1, loaded before question derivation, with SKILL.md granting it explicit authority ("This dictates the intro shape for THIS video", craft note 5: "The format planner is the adaptation layer"). In the only scenario at issue (45+ min Deep Dive), the model derives 5 questions per the planner and the line-163 mapping rule applies to the locked questions; its parenthetical states the intent is title/thumbnail alignment, not the literal count. The claimed outcome (model flags its own compliant draft) requires reading line 163 in isolation from the planner and from SKILL.md's own two acknowledgments of the 5-thing extension. What remains is wording polish (line 163 could say "the locked viewer questions" instead of "Top 3"), which does not clear the bar for a real finding.

### [cross-file-claims] The reference files' "does NOT restate" contracts are false; they restate intro-architecture content that has already diverged
Citations are all real (verified credibility-line-weaving.md:11,28,54-66; problem-result-options.md:11,23,37,53,118; intro-architecture.md:118-134,204; SKILL.md:365-366), but the finding's load-bearing claim, that the Tease gates have "already diverged", is false. intro-architecture Step 3 already assigns the acute-pain + dramatic-result cell to Combine ("both sides matter... high-emotion AND impressive", lines 133-134), so its Poke/Tease when-to-use lines are overlapping conditions disambiguated by Combine. problem-result-options' matrix (acute+moderate=Poke, moderate+dramatic=Tease, acute+dramatic=Combine) is the identical routing written disjointly; the "moderate pain" condition on Tease is the complement of Combine owning the both-high cell, not a new gate. No input cell yields different recommendations from the two files, so the claimed concrete outcome (same video, different recommendation depending on copy consulted) cannot occur. The remaining "restatement" evidence is the files doing their declared job: line 11 of each file names vocabulary and promises "runtime calibration examples" / "examples-first contrastive", and the bolted-on near-miss (lines 54-66) is a NEW example with different material, not a copy of the parent's Bob example; placement is inherently bolted-vs-woven so overlap is by design. The single verbatim duplication (the $6M example sentence, credibility-line-weaving.md:28 = intro-architecture.md:123) is an illustrative example, not a rule; if the parent example changed, the copy stays self-consistent and no output degrades. What survives is a wording nit ("does NOT restate either" could read "does not restate the definitions"), which does not clear the concrete-bad-outcome bar. Verdict: false-positive.

### [cross-file-claims] Banned-phrase severity conflict between two always-loaded files: intro-architecture hard-frictions phrases the SKILL and transition-bank tier as creator's-choice
Evidence is accurately cited (SKILL.md:172-173/:296 tier B-4/B-5 as Tier 2 creator-decides; intro-architecture.md:180-181 lists both in an untiered banned table under a "Hard friction (almost always wrong)" header at :288; the table omits B-6). But both claimed outcomes fail on the actual text. (1) The auto-reject risk is refuted by intro-architecture.md:282, the preamble governing the entire anti-patterns section: "The skill should flag them and explain why. Not auto-reject. The creator can override if they have reason to," reinforced at :298 ("Locking everything as 'REJECT' stifles creativity"). intro-architecture never authorizes auto-rejecting anything; it is uniformly SOFTER than the SKILL, so the auditor's harm direction is backwards. Under both files the runtime behavior for B-4/B-5 converges: surface, explain, creator decides. The only delta is flag tone. (2) The B-6 miss requires the model to ignore the SKILL it is executing (auto-reject stated twice at :172 and :296) plus the unconditionally loaded transition-bank Section 4 (SKILL.md:56, template :90); and vid-pressure-test re-audits all transitions against Section 4 (template :111) as a downstream backstop. What remains is a label drift between a duplicate table and its canonical tiered source, with no nameable behavioral consequence. Adding tier labels to intro-architecture's table would be polish. No concrete bad outcome, so per the severity bar this is a false positive.
