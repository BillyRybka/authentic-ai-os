# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Obsidian workspace state (keep config, ignore local UI state)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Logs
*.log

# Claude local
.claude/settings.local.json
.claude/.credentials.json
.claude/skills/*-workspace/

# Work-in-progress skills (prefix with _ to keep out of releases)
.claude/skills/_*/

# Billy's strategy + working files (never ships to clients)
build-plan.md
*.log
_scratch/
_strategy/
_notes/
# Dev scratch inside skills: machine paths + named third-party refs. Never ships.
*WORKING-NOTES.md
.claude/voice-audit-log.md

# Creator vault data. NEVER ships. The product is the plugin, not Billy's content.
# Clients get an empty vault scaffolded by creator-setup at runtime.
foundation/
Content/
People/
Companies/
Resources/
raw/
Notes/

# Banks: the STRUCTURE + schema READMEs are product and ship.
# The actual entries (and assets) are personal content and never ship.
banks/*/**
!banks/*/README.md
banks/*.md
# Any test/working vault run from inside the repo (recommended: run it OUTSIDE the repo).
Authentic-AI-OS/

# Temp / scratch artifacts
.tmp-*
diagram-preview.png

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/
